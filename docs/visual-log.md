# 見た目の改善ログ（隠れ場所）

**1サイクル = 1項目。** 撮って測って `visual-judge` が採点し、
悪化したら戻す。基準は `docs/visual-gate.md`（動かさない）。

「基準外」は V1〜V6 のうち基準を満たさない項目の数（29箇所 × 6項目 = 174 のうち）。

| # | 変えたこと | 基準外 | 悪化 | 採否 | 備考 |
|---|---|---|---|---|---|
| 0 | baseline | 56 | — | — | 平らな面の内部 L\* 四分位範囲が 0.00〜0.35（のっぺり）|
| 1 | 頂点カラーで陰影を焼き込む（`FORM_SHADING`）| 42 | 10 | 採用 | V3 が全箇所で改善（0.93→0.38 など）。V2 は 0→3 台まで。ouchi/huton の V1 が 3.27→0.55 に悪化（未解決） |
| 2 | 手続き生成の表面テクスチャ（`SpotTextures.ts`）| 32 | 7 | 採用 | 板目・木目・布の襞・石のまだら・葉。**隠れ場所ごとに種を変える**。貼ると暗くなるぶんは色を 1/平均 倍して打ち消す |
| 3 | 落ち影を本体の外接箱に合わせる（`DROP_SHADOW`）| 40 | 12 | 採用 | 途中で2回踏んだ: ①ワールド座標のまま使って影が二重に持ち上がった ②倍率で作ったら本体の裏に隠れて V5 が 0 になった。**上下の段で視差の符号が逆**なので見かけの位置から逆算する |
| 4 | 背板を輪郭の内側に収める・楕円にする／岩の内側の板を詰める | 37 | 11 | 採用 | 背板が平らな長方形として見えていた。岩の板は W×1.1 → W×0.78（0.62 まで詰めたら遮蔽テストが 5点の漏れを捕まえた。高さは詰めない）|
| 5 | 模様の細かさ `SURFACE_TILE` 1.2 → 0.85 | 36 | 11 | 採用 | 面1枚に模様が1回ぶんしか乗らず、平らな部分が残っていた |
| 6 | 模様の下地を白から中間の灰へ／構造の明暗を広げる | 30 | 8 | 採用 | **白の上に白を重ねても何も変わらない**ので、模様が暗くする側しか効いていなかった。V2 が はこ 3.59→7.97 / とびら 6.05→10.99 / こや 6.08→14.27 |
| 7 | 場面の光の色を背景の絵から取る（`SCENE_LIGHT_TINT`）| 33 | 17 | **単独では棄却** | 灰色の岩は改善（-42→-38）したが、もともと派手な カーテン・くさむら をさらに押し上げた（11.7→18.4）。彩度のずれは両方向なので、光だけでは片側しか直らない |
| 8 | 光の色 ＋ 配色の彩度を両方向に寄せる | 20 | 6 | 採用 | 岩に土の色を入れ（C\* 3→14）、くさむら・カーテンの彩度を下げた。V4 の落第 13 → 5（残るのは岩5つ）|

**採点の測り方を1つ直した（サイクル9の途中）。** 落ち影とハローが
「隠れ場所の画素」の切り出しに混ざって、輪郭の明度差が壊れていた。
`setShadowsVisible` でハローも消し、輪郭の外側は**画面に出ている絵**から取る
（背景だけの1枚は切り出しにしか使わない）。**閾値は変えていない。**
同じコードで baseline を測り直すと 55件（前の測り方では 56件）。

| # | 変えたこと | 基準外 | 悪化 | 採否 | 備考 |
|---|---|---|---|---|---|
| 9 | 輪郭のまわりに暗がり（ハロー）を回す | 44 | 30 | **棄却** | 暗い隠れ場所では背景を暗くして**逆に輪郭を消した**（はこ V1 26.2→2.3）。明るい隠れ場所にしか効かない |
| 10 | 楕円の影を濃くする | 25 | 16 | **棄却** | 同じ理由（背景まで暗くする）|
| 10b | 丸い隠れ場所だけ影を下へ多くずらす | 18 | 9 | 採用 | 丸い輪郭は下へすぼまるので、同じずらし量だと影が裏に入る |
| 11 | 焼き込む陰影の幅を 0.30 → 0.44 | 19 | 11 | **棄却** | 輪郭の明度差は上がらず、内部の分布だけ荒れた |
| 11b | 光の色を「色みだけ寄せて明るさは戻す」 | 22 | 11 | **棄却** | 輪郭の明度差は少し戻ったが、彩度差 V4 が岩で -28→-32 と悪化した |
| 12 | 面に**横方向の勾配**を付ける／主光を左へ | 20 | 10 | 採用 | 判定の「次に直すべき1件」。板の左端と右端が 0.3/255 しか違わず、木目を重ねても面は平らに見えていた。数値は 18→20 と増えたが、**数値のほうが抜け道を通っていた**（判定が指摘）|
| 13 | 落ち影を輪郭に寄せる（blur 0.18 → 0.14）| 20 | 12 | 採用 | 影の 52.6% が地平線より上の空に落ちていた（判定の実測）|
| 14 | 場面の光の色を寄せる強さ 0.62 → 0.38 | 19 | 11 | 採用 | 明度が背景に近づいて輪郭が消えていたぶんを戻す |
| 15 | 背景の絵の明るさを読んで隠れ場所の値を離す | 21 | 14 | **棄却** | 比べていたのが**素の色**（albedo）で、画面に出る明るさ（光を掛けたあと）ではなかった。比較の相手が違うので効かない |

| 16 | 模様を材質ごとに描き分ける／岩の内側の板を楕円に | 19 | 9 | 採用 | すいめんに斜めの光の筋とうねる波（「濃い青の板」だった）／布の襞を柔らかく（トタン板に見えていた）／素焼きの斑点をやめて焼きむらに／石の粒を細かく（迷彩柄に見えていた）／岩の板を楕円に（空を背に長方形が見えていた）|

| 17 | 岩の上の板を、瘤を重ねた不定形の岩棚にする | 20 | 10 | 採用 | 判定の「次に直すべき1件」。両端を直角に切った長方形が岩より 30px 突き出していた。**芯の幅は詰めすぎない**（W×0.78 で遮蔽テストが 8点の漏れを捕まえた）|
| 18 | 布の襞を不揃いにする／背板の形を前面に合わせる | 21 | 11 | 採用 | 等間隔の襞は「トタン板」に見えていた。四角い隠れ場所に楕円の背板を当てると、前面からはみ出した弧が黒い三日月として覗く（判定の実測）|

| 19 | 横方向の勾配を 0.26 → 0.58 に強める | 16 | 10 | 採用 | 0.36→19件 / 0.46→17件 / 0.58→16件。判定が「面の明暗を模様ではなく光の向きに沿った勾配で作れ」と言ったところ |

| 20 | 岩の色を灰から砂岩へ | 15 | 11 | 採用 | 背景画との彩度差が -30.6/-27.6/-25.6/-24.1/-23.8 → -18.1/-14.4/-12.2/-11.9/-11.9。どうぶつえん と きょうりゅう は基準内に入った |

| 21 | 岩の庇を「板＋瘤」から**岩の塊だけ**に | 15 | 11 | 採用 | 芯の板を残した版は判定で「上下の縁が平行な直線の梁のまま」と同じ減点が残った。直線を1本も残さない。横に広げすぎると空に翼が生えて見える（W×0.98 → W×0.62）|
| 22 | 板目を粗く・継ぎ目の光る筋を弱く／素焼きの溝を不揃いに | 16 | 11 | 採用 | 「波板シャッター」「旋盤で挽いたプラスチックの鉢」と言われたところ |
| 23 | ふとんをキルトの模様に（カーテンと分ける）| 14 | 11 | 採用 | 掛け布団に縦の襞を当てていて「塗装した波板」に見えていた |

| 24 | 岩の色をもう一段だけ砂岩へ | 12 | 11 | 採用 | 彩度差 V4 の落第が 3 → 1（そと の いわ -13.19）。もう一段押すと数値は同じで絵が「じゃがいも」になったので手前で止めた |
| 25 | くさむらの株ごとの差を大きくする | 12 | 11 | 採用 | 位置 ±0.08・大きさ +30%・回転 ±0.45rad。±0.035 と +14% では判定に2回続けて「同一」と言われた |

| 26 | 明るい背景の前の隠れ場所を暗く落とす（逆光）| 15 | 12 | **棄却** | 対象の3箇所は良くなるが、もともと足りていた箇所が背景に寄って全体では悪化 |

| 27 | 岩の「塞ぐ板」をやめて、球を6→8個に増やして隙間を埋める | 12 | 10 | 採用 | 板は角丸にしても楕円にしても詰めても、空を背に面として見えた（詰めると遮蔽が漏れる）。**塞ぐものが球なら、はみ出しても岩に見える**。8個を1つのジオメトリに結合したので draw call はむしろ baseline より減った |

| 28 | くさむらの振り幅を +30% → +17% に戻す | 12 | 10 | 採用 | 広げすぎて上の3株が画面上でつながり、**押す場所が3つあることが読めなくなった**（連結成分 5 → 3、いちばん大きいものが 84,119px）|

| 29 | 主光の向きを、背景の絵のいちばん明るいところに合わせる | 11 | 6 | 採用 | きょうりゅう の夕日は画面の左下にあるのに、どの場面でも左上から当てていた。**下からは当てない**（上向き成分に下限）|
| 30 | 空に掛かる落ち影を弱める | 11 | 6 | 採用 | のうじょう の こや の影の 52.6% が地平線より上の空に落ちていた。絵から地平線を読んで 0.34 倍に。段差の小さい絵（うみ）では地平線を決めない |

| 31 | 縦の勾配を「外接箱」ではなく「縁の範囲」で割る／幅を 0.30 → 0.50 | 12 | 8 | 採用 | 判定の「板の面に上から下への勾配が無い」。柱や蓋を含む外接箱で割ると、前板の中では勾配が乗らない。実測（上30%と下30%の差）: きのほら +1.1 → +4.1、きかぶ +1.5 → +4.4、とびら **−1.2 → +2.4**、こや **−4.1 → −0.3**、はこ +1.7 → +4.6。**数値は 11 → 12 件**（うみ の つぼ の V1 が 7.76 で基準割れ）。判定の -15 と引き換えなので採る |

| 32 | 補助光を足す（主光と反対側から弱く）| 12 | 9 | 採用 | 右を向いた側面が二重に暗く、ふとん の右辺 14px が L\* 6.7、すいめん の右辺 18px が L\* 13.1 で「太い黒縁」に見えていた。縦の勾配は保たれている（実測 +4.3〜+9.7）|

| 33 | くさむらの上のふくらみを株ごとに「高く細く／低く広く」振る | 12 | 9 | 採用 | 横には広げない（隣とくっつく）。輪郭の上半分が株ごとに変わる |
| 34 | **上の段を地面まで支える柱を立てる**（見た目の追加）| 11 | 8 | 採用 | 判定が4場面で -12 を付け続けていた「上の段が地面に接していない」への手当て。背板より奥に置くので動物にはかからない。**吊り下がっているもの（カーテン）には立てない**。地平線が読めない絵（うみ）でも立てない。途中で2つ踏んだ: ①`ROWS * 0.2` を添字にしていて常に「地平線なし」を返していた ②組み立て中は `runtime.group` のワールド行列がまだ入っておらず、外接箱がローカル座標のままだった |

| 35 | 柱に陰影・接地影・地面への食い込みを足す | 12 | 9 | **棄却** | 判定が挙げた柱の3欠点（単色・地面の手前で終わる・足元に接地の暗さが無い）を直したが、数値は 11 → 12 と悪化 |
| **34・35 を撤回** | 柱そのものをやめる | 11 | 8 | — | 判定 78 → 67。goal の「悪化したら即revert」に従い、柱を入れる前の最良の状態に戻した |

| 36 | 焼き込む陰影に上下限を入れる | 11 | 8 | 採用 | **「太い黒縁」の正体は3つの項の掛け算だった。** 右下奥の角で 0.75×0.71×0.82＝0.437（各項は 25/29/18% のつもり）。実測: ふとんの最暗部 L\* 6.7 → **15.8**、すいめん 13.1 → **19.7**。補助光で消えなかったのは原因が光ではなかったため |
| 37 | くさむらのふくらみの数と束の回転を振る | 12 | 9 | **棄却** | 判定の「のはら の5株が同じ形」。どの振り方でも `dobutsuen/takaki` の V1 が 9.43 → 7.03 に落ちた（株の形を変えると上端の明度分布が動く）。回転だけの版でも 12/9。`SpotShapes.ts` をまるごと戻した |
| 38 | 石の模様の斑点を、黒白から土と砂の色にする | 11 | 8 | 採用 | **無彩色の斑点は、平均の彩度を灰へ引っ張る。** 素の色をこれ以上濃くすると「じゃがいも」に見えた（サイクル24）ので、斑点の側を色づけた。岩の仲間5箇所すべてで V4 と V1 が改善: `soto/iwa` V4 **−14.7 → −14.31** / V1 23.74 → 24.03、`umi/umiiwa` V4 −10.08 → −9.65、`noujou/saku` V4 −8.43 → −7.99・V1 5.82 → 6.07、`dobutsuen/iwaba` V4 −8.4 → −7.99、`kyoryu/ooiwa` V4 −8.05 → −7.6。引き換えは `umi/umiiwa` の V6 のみ（0.45 → 0.473）。**一度 −13.19 と比べて「悪化」と読み違えた**。−13.19 はサイクル36より前の値で、36 が焼き込みに上下限を入れた時点で −14.7 に動いていた。**比べる相手は記憶ではなく、直前にコミットした `score.json`** |
| 39 | 岩の配色を、明度を変えずに色相だけ橙へ寄せる | **10** | 8 | 採用 | **彩度は「暗くする」以外の軸で上げられる。** サイクル24 は素の色を暗くして「じゃがいも」になったが、L\* を 56.1/69.9/39.8 に固定して色相を 83° → 74° に振ると、C\* だけが 27/30/21 → 33/36/27 に上がる（黄土＝オーカー）。岩の仲間5箇所すべてで V4 が縮んだ: `soto/iwa` **−14.31 → −9.78（基準内に入った）**、`umi/umiiwa` −9.65 → −4.98、`noujou/saku` −7.99 → −2.87、`dobutsuen/iwaba` −7.99 → −3.65、`kyoryu/ooiwa` −7.6 → **−1.97**。**基準外れが 11 → 10 件に減った（サイクル17以来の前進）**。引き換えは既に落ちている2件が少し深まっただけ（`noujou/saku` V1 6.07 → 5.54、`umi/umiiwa` V6 0.473 → 0.489）で、V2 は 11.5〜12.5 と下限 8.0 に余裕がある |
| 40〜44 | **隠れ場所の明るさを、その場所の背景の絵に対して決める**（値の composition）| 14〜17 | 9〜12 | **棄却（5回試して保留にした）** | 下の「値の composition は、輪郭を絵として読まないと steer できない」に実測を残した。**落ちている6箇所が1つも動かないことが分かったのが収穫**で、これは構造的な変更の根拠になる |
| 45 | 空に掛かる影を、**板ぜんたいではなく高さごとに**消す | 10 | 8 | 採用 | **`material.opacity` に掛けていたので、地面に乗るぶん（＝接地の手がかり）まで薄まっていた。** それでいて判定の実測では影の 43.8〜51.9% がまだ空に乗っていた ——「薄くしただけで、空から消えてはいなかった」。頂点アルファ（`fadeShadowOverSky`・板を縦6分割）にすると、地平線をまたぐ2箇所の V5 が `noujou/koya` 7.73 → **23.07**（baseline 20.44 より上）、`noujou/wara` 7.32 → **21.55**（同 17.59）に戻った |
| 46 | 空に乗るぶんを 0 まで消す（`SKY_SHADOW_FADE` = 0）| 14 | 12 | **棄却** | 上の段4箇所は影が**まるごと地平線より上**にあるので、0 にすると影が1枚も残らず V5 がちょうど 0 になった（`ouchi/hako` `ouchi/kaaten` `kyoryu/shida` `kyoryu/ooiwa`）。0.34 に戻した |
| 47 | **屋内の場面では地平線を決めない**（`SceneConfig.indoor`）| 10 | 8 | 採用 | 46 で「なぜ おうち に地平線があるのか」に気づいた。絵の行ごとの明るさの段差がいちばん大きいのは **床と壁の境目**（おうち は v=0.563・段差 20.2・上は明るい壁 189.8）。**壁に落ちる影を弱めてはいけない。** `ouchi/hako` V5 7.50 → **22.62**（baseline 18.19）、`ouchi/kaaten` 7.82 → **23.50**（同 16.57）。絵から屋内を見分ける手は無かった（壁の上側のばらつき 8.1 と きょうりゅう の夕空 6.9 が区別できない）ので**場面が持つデータ**にした。**baseline から下がった項目が 50 → 48 件**（この指標が初めて減った）|
| 48 | 岩とくさむらの輪郭を**両方**作り分ける（岩は上を平たく広く・左右の塊を不揃いに／草は葉の幅を交互に）| 11 | 9 | **棄却** | 草側を触ると `dobutsuen/takaki` が 8.06 → **7.56** と新しく落第した（サイクル37 と同じ箇所。V1 がちょうど 8 にいるので、くさむらの形に触ると必ず線を越える）。V2 はくさむら全部で上がり（13.64 → 14.02 など）`kyoryu/shida` V1 も +2.8 だったが、落第が増えるので採らない |
| 49 | **岩だけ**作り分ける（上の塊を横 1.22倍・縦に潰す／左右の塊の大きさを最大 1.14倍まで不揃いに）| 10 | 8 | 採用 | 「同じ輪郭に色と斑点を変えただけ」（判定 −8、7場面中6場面）への手当て。**作り分けは片方を動かせば足りる**ので、落第すれすれのくさむらには触らない。`noujou/saku` V1 5.54 → **5.88**（落ちている項目が改善）、`dobutsuen/iwaba` +0.5。**baseline から下がった項目が 48 → 47 件**。引き換えは `kyoryu/ooiwa` V1 −0.4 と岩2箇所の V2 −0.3（11.3 と 12.9 で下限 8.0 に余裕）|
| 50 | 掛け布団に**襞と小口**を足す（判定の −15「布団の厚みも折り目も無い」）| 14 | 10 | **棄却** | 襞が**別の島として切り出された**（採点表に `ouchi/huton` が2行出た＝画面上で布団本体とつながって見えていない）。小さい島のほうは V1 5.78・V2 2.46・V3 0.375 で、数値がまるごと悪化した。**面の手前に板を足すやり方では、厚みではなく「貼り付けた別の板」になる。** 厚みを出すなら面そのものを曲げる（格子を増やして頂点を動かす）必要があり、それは `setOpen` の回転と遮蔽のテストに関わるので別途 |
| 51・52 | 鉢の輪を**不揃いの間隔**にして、溝のすぐ上に明るい稜線を置く（判定の −10「素焼きではなく成形プラスチックのバケツ」）| 10 | 8 | **棄却（人間の判断待ち）** | `(i * SIZE) / 6` に 14px の揺らぎを足しただけでは等間隔に見えていた（CLAUDE.md の「等間隔に刻むとプラスチックになる」が残っていた）ので、前の輪からの間隔を毎回 24〜62px で引き直し、溝を土の色に、そのすぐ上に明るい稜線を置いた。**鉢の仲間5箇所すべてで V2 が +0.6〜+1.2、V4 も 0 に近づいた**（`soto/hachi` 14.94 → 15.71 / −3.71 → −3.14 など）。ところが V1 が下がり（`soto/hachi` −0.95、`dobutsuen/hachi` −1.84、**落ちている `umi/tsubo` が 6.52 → 6.16 と深まる**）、baseline から下がった項目も 47 → 48 件。稜線を半分に弱めても数値はほぼ同じだったので、**V1 の低下は稜線ではなく「面が起伏して見えること」そのものの代償**。下の「輪郭の明度差は、勾配を強くするほど通しにくくなる」が、3つめの指標の組でも出た |
| 53 | 主光の**上向き成分の下限**を 0.35 → 0.20 に下げる | 10 | 8 | 採用 | 判定の −4「太陽より上に浮いている物体が、太陽と反対向きに陰を持っている」。きょうりゅう の夕日は絵の左下（x≈42, y≈411）にあるのに、しだ・おおいわ の明部の重心が本体中心より dy = −42 / −54（上側）だった。下限に当たっていたのが原因。**変わったのは きょうりゅう だけ**（他の場面は太陽が高くて下限に当たらない）。V1 が4箇所のうち3箇所で上がり、**落ちている `kyoryu/tamago` が 2.66 → 3.03**、V4 は4箇所すべてで 0 に近づいた。**0 にはしない**（「下からは当てない」は人間が決めたこと）|
| 54 | 水の縁の波を、**隣と重ねて高さを1つずつ変える**（判定の −15「直角の長方形の板」）| 11 | 9 | **棄却（色が原因）** | 高さ 0.05・等間隔5個では画面で水平な直線だった。重ねて 0.10〜0.17 に振ったら波にはなったが、泡の色（`accent` = 淡い水色）のまま大きくしたので上の縁に明るい帯ができ、**V1 が 14.79 → 6.25 と落第**した |
| 55 | 同じ波を、**水そのものの色**（`cover`）で作る | 10 | 8 | 採用 | 波の形はそのままで、色だけ変えた。`umi/suimen` V2 9.60 → 9.95、V1 −0.68（14.11 で下限 8.0 に余裕）、V5 −1.05（20.78 で下限 3.0 に余裕）。**基準外れ・悪化・下がった項目はすべて変わらない**。泡の白は模様（`SpotTextures.water`）の側で出す。分割を (10,6) → (8,5) に落として個数を 5 → 8 にしたので三角形は +12（うみ は baseline 比 +7.8%。予算 +10%）|
| 56〜61 | **焼き込む陰影を、倍率ではなく L\* で足す** | 10〜13 | 8〜10 | **棄却（人間の判断待ち。下に詳しい）** | 判定の −15 が2場面（「平らな青いカード」「直角の長方形の板」）。**画素を測ったら、前板の上下の勾配が `ouchi/huton` で +2.49 L\*、`umi/suimen` で +4.37 しか無かった。** 原因は倍率で焼いていたこと |

## 判定（`visual-judge`）の推移

| 回 | 全体 | いちばん悪い場面 | 次に直すべき1件 |
|---|---|---|---|
| baseline | 57点 | ouchi 57 | 平らな面に形に沿った明暗を付ける |
| サイクル16後 | 57点 | kyoryu 57 | 面の明暗を模様ではなく光の向きに沿った勾配で作る |
| サイクル20後 | 48点 | kyoryu 48 | 岩の上の板を**形ごと**外す |
| サイクル25後 | **69点** | kyoryu 69 | 岩の後ろの平板を本当に外す（サイクル27で実施済み）|
| サイクル28後 | 65点 | kyoryu 65 | 板の面に上から下への勾配を入れる |
| サイクル31後 | **78点** | ouchi 78 | 上の段を背景の地面・支えに接触させる |
| サイクル34後 | 67点 | ouchi 67 | 柱そのものを直す（柱が単色・地面の手前で終わる・足元に影が無い）|

**最終状態はサイクル33まで（＋32の補助光）。** 柱（34・35）は撤回したので、
判定 78点 の構成に、サイクル32（補助光）・33（株ごとの形）が乗ったもの。

**サイクル20後に一度下がっている**のは、岩の板を「板＋瘤」にした版が
「上下の縁が平行な直線の梁のまま」と読まれたため。サイクル21で形ごと外した。

## 保留（5回試して動かなかった）

**V1（輪郭の明度差 p10）と V6（消える輪郭の割合）の6箇所** —
おうち の とびら・ふとん、うみ の いわ、のうじょう の こや・さく、
きょうりゅう の きかぶ・たまご。

試したこと（すべて棄却）: ①輪郭のまわりの暗がり ②焼き込む陰影の幅を広げる
③光の色を明るさだけ戻す ④落ち影を濃く ⑤背景の明るさから値を離す
⑥明るい背景の前で暗く落とす（逆光）。

### なぜ動かないのか（2026-09-08 の実測）

7箇所の「隠れ場所の L\* の範囲」と「輪郭のすぐ外の背景の L\*」を並べて測った。

| 場所 | 隠れ場所 L\* | 背景 L\*（中央） | 背景の中央値が隠れ場所の範囲の中 |
|---|---|---|---|
| おうち/とびら | 27.2–58.0 | 37.7 | **はい** |
| うみ/いわ | 18.4–56.3 | 51.2 | **はい** |
| きょうりゅう/きかぶ | 15.3–34.8 | 25.2 | **はい** |
| きょうりゅう/たまご | 14.6–45.3 | 33.9 | **はい** |
| のうじょう/こや | 21.2–59.2 | 68.5 | いいえ |
| のうじょう/さく | 22.3–58.4 | 65.3 | いいえ |
| うみ/つぼ | 10.9–42.3 | 48.3 | いいえ |

**上の4箇所は原理的に通せない。** 背景の明るさが隠れ場所自身の明暗の
範囲の内側にあるので、輪郭のどこかは必ず背景と同じ明るさになる。
V1 は輪郭のいちばん弱い 10% を見るので、そこが引っかかる。

しかも**形に沿った勾配を強くするほど、隠れ場所の明暗の幅は広がる**ので、
判定が求める「面の明暗を勾配で作る」と V1 は互いに引っ張り合う。

下の3箇所は離せる余地があるが、⑥で試したところ、対象は良くなる代わりに
もともと足りていた箇所が背景に寄って**全体では悪化**した（12 → 15件）。

**判定は baseline の時点で「絵では4つとも一目で見つかる。色相で分離している。
よって −20（埋もれて分からない）は適用していない」と書いている。**
V1/V6 は明度差しか見ないので、色相だけで分かれている組を落第にする。
基準を動かすのは人間の判断なので、ここは触らずに残す。


## 値の composition は、輪郭を絵として読まないと steer できない（サイクル40〜44）

残り10件はすべて V1（輪郭の明度差）と V6（消える輪郭の割合）で、
原因は「隠れ場所と、その場所の背景の絵が、たまたま同じ明るさ」。
**絵描きなら背景が暗いところに置く物を明るくする**ので、それを機械でやってみた。
`sampleBackdropValue()`（絵の一部の L\* を読む）と
`VALUE_COMPOSE`（離す量の上限・暗くする側の下限）を足して、5通り試した。

| 回 | やったこと | 基準外れ | 分かったこと |
|---|---|---|---|
| 40 | 真後ろを1箇所読んで、**平均から**離す | 17 | **9箇所で V1 が +5〜+7 効いた**（はこ +5.1、いわ +7.2、わら +5.9、のはら の株 +7）。ところが**落ちている6箇所には1つも効かず**、通っていた3箇所を落とした（`noujou/okego` 13.96 → 9.26、`dobutsuen/hachi` 12.25 → 7.52）。輪郭は明るさの違う背景を**いくつも横切る**ので、平均から離すと別の帯に突っ込む |
| 41 | 輪郭の上を24点読んで、**いちばん近い背景から最も遠い**明るさを選ぶ（V1 の p10 が見ているものそのもの）| 15 | `noujou/saku` 5.54 → 7.53、`kyoryu/kikabu` 0.66 → 1.98 と、**落ちている箇所が初めて動いた**。代わりに `dobutsuen/takaki` が 8.06 → **3.43** と新しく落第 |
| 42 | 「既に離れている場所は触らない」門（`skipAbove` = 14）を足す | 15 | **1件も変わらなかった。** 24点の楕円だと、どの隠れ場所でも「いちばん近い背景」は必ず近い（隙間の奥を読んでいる）ので、門が働かない |
| 43 | **丸い塊（`ROUND_SHAPES`）には掛けない**。板の隠れ場所は輪郭が外接箱とほぼ一致する | 13 | 下がる箇所は消えて、動いた5箇所すべてで V1 が +3〜+8。ところが**輝度に倍率を掛けたせいで V2 と V4 が道連れに落ちた**（`ouchi/kaaten` V2 9.05 → 7.49、`soto/hora` V4 -10.86 → -14.66）|
| 44 | 倍率をやめて **L\* 空間で足す**（`shiftLStar`。a\*, b\* は触らない）| 14 | V4 の道連れは直った（`kaaten` 5.61 → 6.2）が、ふとん（濃い青）は**色域の外に出て**彩度が落ちた（V1 15.11 → **5.8**、V4 -6.73 → **-14.99**）。`kaaten` の V2 は倍率でも L\* 足しでも 7.4 に落ちる |

**ここまでで確定したこと（構造的な変更の根拠）:**

1. **落ちている6箇所は、±9 L\* の範囲に「背景から離れる明るさ」が1つも無い。**
   43 以降、`ouchi/tobira` / `umi/tsubo` / `umi/umiiwa` / `noujou/saku` /
   `kyoryu/kikabu` / `kyoryu/tamago` は**1件も動かなかった**
   （最適値を探して、いまの値がすでに最適だった）。
   `maxShift` を広げれば動くが、それは場面の配色を壊す量になる
2. **外接箱の楕円は、丸い塊の輪郭の近似にならない。**
   隙間の奥を読むので、動かすほど下がる側に転ぶ（42 で門が働かなかったのも同じ理由）
3. **明るさを動かすと、V2（面の明暗の幅）が道連れになる。**
   `shiftLStar` で L\* 空間で足しても `kaaten` は 9.05 → 7.42。
   色域の端（濃い青のふとん）では彩度まで落ちる

**人間への提案（実行していない）。**
この路線をまともに steer するには、**隠れ場所の輪郭を絵として読む**仕組みが要る:
場面を組んだあとオフスクリーンに隠れ場所だけを1枚描いて外周の画素をなぞり、
その画素の位置で背景の絵を読む。そうすれば 24点の楕円ではなく**本当の輪郭**の上で
背景を読めるので、丸い塊にも掛けられるし、`skipAbove` の門も働く。
ただしこれは「場面の構築にオフスクリーン描画を1枚足す」という構造の追加で、
`SceneRoot.build()` がレンダラに依存することになる（いまは依存していない）。
goal の「構造的な大きい変更が必要なら提案して止まる」に当たるので、
**判断は人間に委ねる。**


## 描画量（合格条件6）を実測した（2026-09-13）

baseline のコミット（`778e2e1`）を実際に checkout して、同じ e2e
（「どの場面でも起動して、どこを押しても反応が返る」）を回して比べた。

| 場面 | 三角形 baseline → いま | | draw call baseline → いま | |
|---|---|---|---|---|
| ouchi | 840 → 840 | ±0.0% | 35 → 35 | ±0.0% |
| soto | 3,830 → 4,202 | **+9.7%** | 66 → 59 | −10.6% |
| umi | 4,483 → 4,821 | +7.5% | 79 → 70 | −11.4% |
| noujou | 4,044 → 4,420 | +9.3% | 66 → 61 | −7.6% |
| dobutsuen | 4,044 → 4,418 | +9.2% | 66 → 60 | −9.1% |
| kyoryu | 3,876 → 4,252 | **+9.7%** | 59 → 54 | −8.5% |
| nohara | 8,800 → 8,850 | +0.6% | 96 → 96 | ±0.0% |

**合格条件6（baseline の +10% 以内）は満たしている**が、いちばん重い2場面が
**+9.7% で、残りは 0.3%**。draw call は結合（`mergeGeometries`）のおかげで
baseline より減っている。

**これ以上、形を足す余地が無い。** 次に形で何かを足すなら、
どこかの分割数を落として先に空けること（サイクル45 で落ち影を縦6分割にした
ぶんもここに乗っている）。


## 人間に決めてほしいこと（2026-09-13）

サイクル49 まで回して、**合格ラインに残っている穴は2つとも構造の変更が要る**。
goal の「構造的な大きい変更が必要なら、提案して止まる」に当たるので、
実行せずにここに書く。

### 提案1: 上の段が地面／地平線に接していない（判定 −12 を4場面）

**判定は「これの決着が要る。直しても kyoryu は 78点で合格しない」と書いている。**
つまり**この1件が合格ライン 85点を塞いでいる。**

実測（判定）:
- `kyoryu/shida` `kyoryu/ooiwa` 下端 y=372 / 388 の真下の背景画素が夕焼けの空
  (252,164,87) / (255,183,94)。地面の境目は y≈470 なので **82〜98px 宙に浮いている**
- `noujou/koya` 下端 y=321 の真下が青空 (206,236,253)
- `nohara/kusa1` `kusa2` 下端 y=355、地平線の帯は y≈372
- `ouchi/hako` は床の線 y=479 に対して 163px 上（ただし屋内なので壁。
  サイクル47 で影は戻した）

**原因は見た目ではなく配置。** 隠れ場所は上下2段（y = +1.90 / −2.00）に固定で、
背景の絵の地平線は場面ごとに違う（実測 v = 0.333〜0.563）。
**上の段は、どの絵でも地平線より上に来る。**

選べる道（どれも大きい）:

| 案 | やること | 失うもの |
|---|---|---|
| A | 上の段の y を**場面ごとに**地平線の下へ下げる | 上下の段の間隔が詰まって当たり判定が重なる（CLAUDE.md の実測では 2×2 で既に 2.0px しか空いていない）。§5-3 の配置が場面ごとに変わる |
| B | 背景の絵を描き直して、**地平線を上げる**（空を狭くする） | 絵を7枚描き直す。素材ファイルで解決するのは goal の禁止事項（記録だけしてある） |
| C | 上の段に**地面を足す**（棚・土手・枝など、その段の足場になるもの） | サイクル34・35 で柱を試して棄却している（判定 78 → 67）。足場は「置いた物」に見えやすい |
| D | 上の段は**浮いていてよいものに限る**（ちょうちょ・雲・枝からぶら下がるもの） | 隠れ場所9種のうち上段に置けるものが減る。場面の作り直し |
| E | この減点は**受け入れて、合格ラインを人間が引き直す** | —— |

**どれを採るかは人間の判断。** A〜D はどれも §5-3 の配置と当たり判定の実測
（CLAUDE.md「当たり判定の半径は、定数では決められない」）に跳ね返る。

### 提案2: 輪郭を絵として読んで、値を composition する

上の「値の composition は、輪郭を絵として読まないと steer できない」に詳しい。
残り10件の基準外れ（すべて V1／V6）は、これが無いと動かせないことを
5通り試して確かめた。

### 付随して決めてほしいこと: 合格条件1 と 3 が正面からぶつかる

- **条件1（判定 85点以上）**は「平らな面を立体に見せろ」と言う
- **条件3（baseline より悪化した項目が1つも無い）**は、そのたびに
  V1（輪郭の明度差）が下がるので通らない

理由は測ってある（下の「輪郭の明度差は、勾配を強くするほど通しにくくなる」）:
**形に沿った明暗を強くすると隠れ場所自身の L\* の幅が広がるので、
背景の明るさがその範囲の内側に入る。** サイクル51・52（鉢の輪）でも同じことが
起きて、V2 +0.6〜+1.2 と V1 −0.3〜−1.8 の取引になった。

いまの状態は baseline から **47項目**が下がっている（基準の内側の動きを含む）。
**基準を動かすのは人間なので、ここは触っていない。**


## 倍率で焼いた陰影は、暗い面にはほとんど乗らない（サイクル56〜61）

判定は2場面に −15 を付けていた:
「平らな青いカードの後ろに平らな紺のカード。布団の厚みも折り目も無い」（`ouchi/huton`）、
「直角の長方形の板が2枚。まわりが全部丸い塊の場面に四角い板が1枚混ざっている」（`umi/suimen`）。

**画素を測った**（`capture/visual/current/*.solid.png` と `*.bg.png` の差で
隠れ場所の画素を切り出し、青みで前板と背板に分けて L\* を集計）:

| | 上30%と下30%の L\* 差 | 内部 L\* の IQR |
|---|---|---|
| `ouchi/huton` の前板 | **+2.49** | 3.82 |
| `umi/suimen` の水 | **+4.37** | 4.35 |

`FORM_SHADING.lift` は 0.50（±25%）も振っているのに、画面では 2〜4 L\* しか動かない。

**L\* は輝度の立方根なので、同じ倍率を掛けても L\* の変化はその面の L\* に比例する。**
明るい面（L\* 70）なら ±25% が ±6 L\* ほどになるが、濃い紺（L\* 33）では ±2.7 にしかならない。
CLAUDE.md が baseline で「内部の L\* の四分位範囲が 0.00〜0.35」と挙げた
はこ・カーテン・ふとん・すいめん・こや は、**どれも暗い面**だった。

### 5通り試した

| 回 | 設定 | 基準外れ | 悪化 | 下がった | V2 上/下 |
|---|---|---|---|---|---|
| 56 | L\* 加算（lift 13 / side 15 / depth 4.7、蓋 −10..+8）| 13 | 10 | 51 | 5 / 24 |
| 57 | 同（19 / 21 / 6.2、蓋 −16..+11）| 11 | 8 | 49 | **28 / 1** |
| 58 | 同（16 / 18 / 5.5、蓋 −13..+9.5）| 12 | 10 | 50 | 11 / 18 |
| 59 | 同（19 / **12** / 6.2）左右の項だけ弱める | 12 | 9 | 47 | 24 / 5 |
| 60 | **倍率では足りない面だけ L\* で底上げ**（`weakSwing` = 9）| **10** | **8** | 51 | 20 / 7 |
| 61 | 同（`weakSwing` = 7）| 11 | 8 | 50 | 15 / 11 |

**60 が唯一、基準外れ（10件）も悪化（8件）も増やさない。**
`ouchi/huton` の V2 は 8.06 → 8.43、`ouchi/kaaten` 9.05 → 9.80 と、
判定が −15 を付けた面がまさに良くなる。

### それでも棄却した理由

60 でも、**すでに落ちている項目が3つ深まる**:
`ouchi/tobira` V1 2.75 → 2.21、同 V6 0.218 → 0.232、`kyoryu/kikabu` V1 0.70 → 0.63。
baseline から下がった項目も 47 → 51 件。
サイクル51・52（鉢の輪）を同じ理由で棄却しているので、ここも同じ扱いにした。

### 分かったこと: **下限すれすれの2箇所が、場面全体を触る変更を全部止めている**

`noujou/koya` は V1 **8.54**、`dobutsuen/takaki` は **8.06**（下限 8.0）。
56〜59 のどの強さでも、この2箇所のどちらかが必ず 8.0 を割った。
**場面全体の見た目を動かす変更は、原理的にどれも通らない。**
（`koya` が 8.54 まで下がったのは、サイクル45・47 で落ち影を戻したため。
それ自体は V5 を 7.73 → 23.07 に戻した採用済みの改善で、取り消していない。）

### 人間に選んでほしいこと

**いますぐ適用できる改善が3つ、合格条件に阻まれて棚上げになっている。**

| | 効果 | 代償 |
|---|---|---|
| サイクル51・52（鉢の輪を不揃いに）| 鉢5箇所の V2 +0.6〜+1.2、V4 も改善 | `umi/tsubo` V1 6.52 → 6.16 |
| サイクル57（L\* 加算・強）| **V2 が 29箇所中 28箇所で上がる** | `noujou/koya` が落第（8.54 → 6.79）|
| **サイクル60（弱い面だけ底上げ・`weakSwing` = 9）** | V2 が 20箇所で上がる。**基準外れも悪化も増えない** | 落ちている3項目が少し深まる／下がった項目 47 → 51 |

**60 は `src/data/look.ts` と `src/peekaboo/SpotShapes.ts` の2ファイルだけで、
上の表の設定をそのまま入れれば再現できる。**
採るかどうかは、合格条件3（baseline 比で劣化ゼロ）をどこまで厳密に読むかの判断なので、
**人間が決めること。**
