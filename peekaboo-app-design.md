# いないいないばあアプリ「ばあ！」設計書 v0.2

対象: Claude Code による実装（この1ファイルだけで実装開始できることを目標）
前提: 1歳半の子どもが自分で触って遊ぶ。保護者が横にいなくても壊れない
姉妹プロジェクト: 水族館アプリ「みずのなか」（`suizokukan`）。**そこで実測して分かったことを引き継ぐ**

---

## 0. この文書の位置づけ

「みずのなか」では、いないいないばあは §17-1 の**1機能**だった。
今回はそれが**アプリの全部**になる。だから、あちらで「まあ動いている」で済ませた部分を、
真正面から作り込む必要がある。

**あちらで実際に言われたことが、そのまま今回の要件になる。**

| 実機でのフィードバック（みずのなか） | 今回の要件 |
|---|---|
| 「岩からいないいないばあしてこない」 | **どこに隠れているか、見れば分かること**（§4-2 ヒント） |
| 「住人の存在感が薄い。もっとインパクトある出方に」 | **登場は大きく・速く・音つき**（§5） |
| 「尻尾が少し見えているようにしてください」 | **隠れていても体の一部が見えている**（§4-2） |
| 「触っても何も反応しない」（貝） | **どの瞬間に押しても必ず反応する**（不変条件2） |
| 「切替ボタンと岩が被っている」 | **UI が当たり判定を塞がないこと**（§3-2） |

---

## 1. アプリの狙い

### 1歳半にとっての「いないいないばあ」

この月齢の子が喜ぶ理由は、**物の永続性**が育っている最中だから。
「見えなくなった＝無くなった」ではなく「隠れているだけ」と分かりはじめた喜びが、
あの笑いになる。設計上、これは3つの意味を持つ。

1. **「隠れている」と分からせる必要がある。** 完全に見えなくなっていると、
   そこに何かがあると気づけない。尻尾・耳・鼻先が見えていること
2. **待ちの時間（ため）が要る。** すぐ出ると「ばあ」にならない。
   押した瞬間に反応（音・揺れ）→ ひと呼吸 → 登場、の順
3. **自分で起こせること。** 勝手に出てくるだけだと受け身になる。
   タップが原因、登場が結果、という因果がはっきりしていること

### 繰り返しは仕様であって、飽きではない

1歳半は**同じ場所を20回押す**。だから、
- 20回目も1回目と同じ品質で反応すること（不変条件2）
- ただし**中身は少し変わる**と、さらに続く（§6 飽きさせない仕組み）

### 魚と猫といろんな動物

ご要望どおり、動物の種類が主役。**種類が多いこと自体が価値**になる。
水族館アプリは「1水槽に住人1〜2匹」だったが、今回は
**フィールド4つ × 隠れ場所4箇所**を基本の器にする。

### いちばん効くのは「移動を見せる」こと

**この設計の中心。** 隠れているものを想像するのは1歳半には難しいが、
**移動を目で追うのはできる**。

> 「ばあっ！」→ うさぎが**ぴょんぴょんと隣の隠れ場所へ移動する**
> → その様子を全部見せる → また隠れる

これをやると、物の永続性の学習に直接効くうえに、遊びの構造そのものが変わる。

| 移動が無い場合 | 移動を見せる場合 |
|---|---|
| どこを押すか手がかりが無い | **「あそこに入った」と分かる**。指をさせる |
| 当てても外しても同じ | **狙って当てられる＝成功体験** |
| 「ため」を演出で作る必要がある | **移動時間がそのまま期待になる** |
| ヒント（尻尾のはみ出し）が必須 | 移動そのものが最強のヒント |

だから、**移動するモードは「1フィールドの変わり種」ではなく、
このアプリの主役の遊び方**として設計する（§4-5）。

---

## 2. 幼児向け必須要件（不変条件）

**この章だけは実装より優先する。** 食い違ったら実装を直す。
「みずのなか」の不変条件を引き継ぎ、いないいないばあ用に足した。

1. **画面のどこをタップしても必ず何かが起きる。** 無反応を作らない。
   隠れ場所の上なら動物が出る。それ以外でも波紋・音・近くの動物の反応が出る
2. **どの瞬間に押しても反応する。** 出ている途中・引っ込む途中でも受け付ける。
   **「アニメーション中だから無視」は禁止**（みずのなかの貝で実際にやって、
   連打すると開き量が最大 0.037＝事実上ずっと閉じたままだった）。
   向きを変えないのは構わないが、**声とアピールは必ず返す**
3. **隠れている動物は、必ず体の一部が見えている。** 完全に消えない。
   ただし**移動モード（§4-5）で空になった隠れ場所は例外**。
   そこは「空である」ことを見せてよい（下の 3b で無反応にはしない）
3b. **空の隠れ場所を押しても、必ず反応する。**
   ふたが開いて誰もいないことを見せ、音と煙を出し、
   **そのあと正解の隠れ場所を揺らして知らせる**。
   「押したのに何も起きない」を作らない（実装は §4-6）
4. **登場は 0.35秒以内に始まる。** 押してから何も起きない時間を作らない
4b. **移動中に押しても反応する。** 動物は立ち止まって振り向き、鳴いてから移動を続ける。
   移動そのものは止めない（止めると「どこへ行ったか」が分からなくなる）
5. **スワイプ・ピンチ・ダブルタップ・長押しに機能を割り当てない。**
   例外はペアレンタルゲートの2秒長押しのみ（画面右上 24×24px）
6. **1秒に3回を超える明滅を作らない。** 急激な全画面フラッシュ禁止。
   `prefers-reduced-motion` 指定時は光の演出を弱める
7. **素材が1つも無くてもアプリは起動する。** モデル・テクスチャ・音の
   すべてに手続き生成のフォールバックがある
8. **場面を切り替えるとき、前の場面の geometry / material / texture を dispose する**
9. **音は初期ミュート。** 最初のタップで解禁し、フェードインさせる
10. **広告・外部リンク・課金・テキスト入力欄を子ども向け画面に置かない**
11. **自動マージしない。** マージ判断は常に人間が行う

### 不変条件は数値のテストで押さえる

「壊れても静かに壊れる」ものは目で気づけない。`tests/unit/safety.test.ts` に置く。

- 60Hz で連打しても、登場アニメが**完走する**（開き量・進捗が最大値に達する）
- 連打中のタップも `true` を返す（声とアピールが止まらない）
- 明滅が 3回/秒 を超えない
- 隠れている状態でも、画面に出ている面積が 0 でない

---

## 3. 画面と操作

### 3-1. レイアウト

```
┌──────────────────────────────────────┐
│                                    [･] │ ← 右上 24x24px 保護者ゲート
│                                        │
│      ┌───┐             ┌────┐          │
│      │ 箱 │             │草むら│         │ ← 隠れ場所（**4箇所**）
│      └─╥─┘             └─╥──┘          │
│        尻尾               耳             │ ← ヒント（必ず見えている）
│                                        │
│      ┌───┐             ┌────┐          │
│      │ドア│             │ふとん│         │
│      └───┘             └────┘          │
│                                        │
│  ┌──┐┌──┐┌──┐┌──┐                     │
│  │おうち││そと││うみ││のはら│            │ ← 場面切替（4つ）
│  └──┘└──┘└──┘└──┘                     │
└──────────────────────────────────────┘
```

- **隠れ場所は 2×2 の4箇所**。画面を4分割して、1つあたりを大きく取る。
  指が届かない場所・小さすぎる的を作らないため（§5-3）

- 場面切替は**常時表示**（子ども自身が押して変えるのも楽しみのうち）
- ボタンは最大 72×72px、間隔は `clamp(6px, 2.5vw, 10px)`。**必ず1行**に収める
  （みずのなかで 88px×5 が 390px 幅で2行に折り返し、下部の当たり判定を隠した）
- 文字は使わない。中身はその場面の絵

### 3-2. UI が当たり判定を塞がないこと

**みずのなかで実際に起きた事故。** 切替バーが画面下部の岩に覆いかぶさり、
押しても岩が反応しなかった。今回は隠れ場所が主役なので、必ず守る。

- 隠れ場所の配置は**バーの上端より上**に限定する
- E2E で「全隠れ場所の当たり判定の中心が、バーの矩形と重ならない」ことを検査する
- 隠れ場所どうしの当たり判定も重ならないこと
  （みずのなかで貝と岩が画面上 21px しか離れておらず、岩が先に取っていた）

### 3-3. 操作はタップだけ

- `pointerdown` だけを見る。押した瞬間に反応する
- `contextmenu` / `dragstart` / `gesturestart` / `gesturechange` / `gestureend` /
  `touchmove`（マルチタッチ）/ `dblclick` はすべて `preventDefault`
- **入力を間引かない。** 連打への耐性はプール側で担保する

---

## 4. コアループ（このアプリの心臓部）

### 4-1. 状態遷移

```
hidden ──タップ──> appearing ──> out ──(idle経過)──> hiding ──> hidden
   ↑                  │            │                    │
   └──────────────────┴────────────┴────────────────────┘
        どの状態でもタップは受け付ける（不変条件2）
```

| 状態 | 何が見えているか | タップしたら |
|---|---|---|
| `hidden` | ヒント（尻尾・耳など）だけ | **appearing へ**。声と効果音 |
| `appearing` | 出てくる途中 | 受け付けるが状態は変えない。声だけ返す |
| `out` | 全身。こちらを見ている | `idle` をリセット（長く出ていてくれる） |
| `hiding` | 引っ込む途中 | **appearing へ戻す**（また出てくる） |

**`hiding` から呼び戻せること**が「みずのなか」との違い。
あちらは `retreating` 中は無反応だった。ここでは「もう一回！」に応えるべき。

### 4-2. ヒント（隠れていることを伝える）

隠れている間、**必ず次のどれかが見えている**。

- 体の一部（尻尾・耳・鼻先・足）が隠れ場所の縁からはみ出している
- 隠れ場所そのものが**数秒に1度、小さく揺れる**（0.6Hz 程度。明滅ではない）
- 時々、小さな音（もぞもぞ）

**はみ出す量の決め方。** 「みずのなか」で試行錯誤した結果:
隠れ場所の見かけ半径が、動物の断面を覆っている必要がある。
覆えていないと「隠れていない」状態になり、逆に覆いすぎると気づけない。
**動物の断面の 0.86 倍を隠れ場所が覆い、はみ出しは体長の 15〜25%** を初期値にする。

### 4-3. 登場のタイミング（ここが命）

```
0.00s  タップ
0.00s  タップ地点に反応（波紋・小さな音）      ← 押した実感
0.05s  隠れ場所が「ぷるっ」と震える            ← ため
0.15s  動物が出はじめる
0.35s  「ばあ！」の声 ＋ 全身が出る ＋ 粒子     ← 山
0.50s  出きって、こちらを向く
2.1s   （タップが無ければ）引っ込みはじめる
2.6s   隠れ終わり
```

- **0.35秒より遅い山は「ため」ではなく「反応が遅い」に変わる**
- **0.15秒より速いと「ばあ」にならない**。押したら即出るのは、ただのボタン
- 引っ込むまで **1.6秒**（みずのなかの `OUT_IDLE_SEC` 実績値）。
  短いと見る前に消える。長いと「隠れている」に戻らず次が押せない

### 4-4. 登場の演出（インパクト）

「存在感が薄い」と言われた反省から、**同時に4つ**を出す。

1. **大きさ**: 隠れているときの 1.0 倍 →登場時 **1.15倍にオーバーシュート**してから 1.0 に戻る
2. **音**: 「ばあ！」の声（動物ごとに違う声）＋ 効果音（ぽん、ぴょこ）
3. **粒子**: きらきら or ぽふっと煙。8〜14粒。**全画面フラッシュにはしない**（不変条件6）
4. **視線**: 出きったらカメラの方を向く。**顔と目が見えることが最重要**

### 4-5. 移動モード「おいかけっこ」（§1 の主役）

**フィールドごとに、遊び方を2つのどちらかにする。**

| | モードA「みんなでかくれんぼ」 | モードB「おいかけっこ」 |
|---|---|---|
| 隠れ場所 | 4箇所 | 4箇所 |
| 動物 | **4体**（各所に1体ずつ住む） | **1体だけ**（残り3箇所は空） |
| ばあの後 | その場に戻る | **次の隠れ場所へ移動する** |
| 遊びの芯 | 押すたびに違う動物 | **どこへ行ったかを追う** |
| ヒント | 尻尾・耳のはみ出し（§4-2） | **移動そのもの** |

モードは `SceneConfig.mode` で持つ（§5-1）。
初期リリースは **4フィールド中 1つをモードB** にし、遊ばれ方を見てから増やす。
「移動が主役」なのは遊びの構造としての話で、いきなり全部Bにすると
「毎回1体しか出ない」＝賑やかさが落ちる。両方あるのが良い。

#### 移動の見せ方（ここを雑にすると全部が台無しになる）

```
0.00s  「ばあ！」（§4-3 と同じ）
0.50s  出きって、こちらを向く
1.70s  idle 終わり。**次の行き先の方を向く**       ← 予告 0.4s
2.10s  ぴょんぴょん開始。3〜4回の弧を描くジャンプ   ← 約 0.35s/1跳ね
3.60s  到着。こちらを一度見る                       ← 0.3s「見つけてね」
3.90s  もぐる（隠れ場所が閉じる）
4.80s  hidden。次のタップを待つ
```

- **必ず行き先の方を先に向く（0.4秒）。** 予告なしに動き出すと、
  「消えた」に見えて目で追えない
- **ジャンプは弧を描く。** 直線の平行移動は「滑って移動した」に見えて、
  1歳半の目には追いづらい。上下動があると視線が乗る
- **到着してから隠れるまでに 0.3秒の間を置く。** ここでこちらを見る。
  「ここに入ったよ」の合図
- **移動中の速度は一定にする。** 加減速をつけると追いづらい
- モードBの `idle` は **1.2秒**（モードAの 1.6秒より短い）。
  出ている時間より移動を見せる時間のほうが大事なので、早めに動き出す
- **1周（ばあ→移動→隠れ終わり）で約 4.8秒。** これ以上長いと、
  1歳半は待てずに他所を押す

#### 次の行き先の選び方

```ts
// 直前と同じ場所には戻らない（「動いていない」に見えるため）。
// 隣接（画面上で近い）を 0.6、遠い方を 0.4 で選ぶ。
// 近い方を優先するのは、移動距離が長いと画面を横断して見失うため。
```

- **同じ場所への即戻りは禁止。** 実装は「直前の spot を候補から外す」
- 4箇所しかないので、候補は常に3つ。順番を固定にしない（覚えられると飽きる）
- ただし**完全なランダムにもしない**。3回連続で同じ場所は避ける

### 4-6. 空の隠れ場所を押したとき（不変条件 3b）

モードBでは4箇所のうち3箇所が空になる。**ここが無反応だと、
このアプリでいちばん多い操作が「何も起きない」になる。**

```
空の場所を押す
  ↓ 0.00s  ふたが開く（正解と同じ動き）      ← 押した実感
  ↓ 0.20s  誰もいない ＋「あれ？」の声 ＋ ぽふっと煙
  ↓ 0.50s  正解の隠れ場所が「ぷるっ」と揺れる ← こっちだよ
  ↓ 0.80s  ふたが閉じる
```

- **外れを「失敗」にしない。** 音は落胆ではなく、とぼけた「あれ？」。
  ブザー・×印・暗転は使わない
- **必ず正解を教える。** 1歳半に「探させる」のは早い。
  当てるのが目的ではなく、**移動を目で追うのが目的**
- ヒントの揺れは §4-2 と同じ 0.6Hz。明滅ではない（不変条件6）
- 空の場所を連打しても、上のシーケンスが**毎回最後まで走る**
  （途中のタップで向きを変えない。不変条件2。みずのなかの貝と同じ扱い）

---

## 5. 動物と隠れ場所

### 5-1. データ構造

```ts
/** 遊び方（§4-5） */
export type SceneMode =
  | 'hideout'   // A: 4箇所に4体が住む。ばあの後もその場にいる
  | 'chase';    // B: 1体が、ばあの後に次の隠れ場所へ移動する

/** 場面（おうち / そと / うみ / のはら） */
export interface SceneConfig {
  id: string;
  label: string;              // ひらがな（読み上げ用。画面には出さない）
  mode: SceneMode;
  backgroundUrl: string | null;  // null なら手続き生成
  ambientSound: string | null;
  /** **4箇所**。水槽と同じで、増やすより組み合わせで変化をつける */
  spots: SpotConfig[];
  /** mode 'chase' のときだけ使う。ここに住む1体 */
  runner?: AnimalId;
}

/** 隠れ場所 */
export interface SpotConfig {
  id: string;
  kind: SpotKind;             // 形と隠れ方を決める
  position: [number, number, number];
  scale: number;
  hitRadiusPx: number;        // 画面上の当たり半径（既定 120）
  /**
   * ここに住む動物。複数なら毎回どれかが出る（§6 飽きさせない）。
   * mode 'chase' では**空配列**にする（誰が入っているかは実行時に決まる）
   */
  animals: AnimalId[];
}

export type SpotKind =
  | 'box'        // 箱のふたが開く
  | 'door'       // ドアが開く
  | 'curtain'    // カーテンが左右に開く
  | 'bush'       // 草むらから飛び出す
  | 'rock'       // 岩の陰から回り込んで出る
  | 'water'      // 水面から顔を出す
  | 'blanket'    // 布がめくれる
  | 'hollow'     // 木の洞から
  | 'pot';       // 植木鉢から

/** 動物 */
export interface AnimalConfig {
  id: AnimalId;
  modelUrl: string | null;    // null なら手続き生成
  /** 頭が -z を向いていないモデルは true（自動判定は誤るのでやめた） */
  modelFlip?: boolean;
  /** 体軸まわりの向き。背が上を向かないモデルに指定する */
  modelRollDeg?: number;
  scale: number;
  color: string;              // 手続き生成の体色
  bellyColor: string;
  /** 「ばあ！」の声。null なら共通の声を使う */
  voice: VoiceId | null;
  /** 出かたの癖 */
  style: 'pop' | 'slide' | 'spin' | 'flip' | 'peek';
  /** 隠れているとき、どこがはみ出すか */
  hintPart: 'tail' | 'ear' | 'nose' | 'foot' | 'fin';
}
```

### 5-2. 初期リリースの動物（11種）

魚と猫を軸に、形が**シルエットで見分けられる**ものを選ぶ。

| 場面 | 常駐（4体） | 予備（§6-2 で入れ替わる） |
|---|---|---|
| おうち | ねこ、いぬ、ねずみ、ことり | うさぎ |
| そと | かえる、りす、はりねずみ、ちょうちょ | ふくろう |
| うみ | クマノミ、たこ、かに、ぺんぎん | いるか |
| のはら | **うさぎ 1匹**（モードB） | ねこ、りす（うさぎと入れ替える） |

**予備は「同じ場所から違う動物が出る」ための在庫**（§6-2）。
常駐 4 + 予備 5 + サプライズ用の大物（ぞう・くじら）2 で **11種**。
15種を先に並べるより、**1種ずつ本当に見分けがつくか**を確かめて増やす。

**シルエットで見分けられることが色より重要。**
「みずのなか」で、体型を指定しないと「チョウチョウウオもメダカも同じ魚」になった。
`bodyHeight` / `bodyWidth` / 耳・尾の有無で、**遠目に種類が分かる**こと。

### 5-3. 場面は4つ、隠れ場所は各4箇所

「みずのなか」の水槽4つと同じ構成にする。切替バーの枠数も同じで済む。

| 場面 | モード | 隠れ場所 | 中身 |
|---|---|---|---|
| おうち | A `hideout` | 4（はこ・カーテン・ふとん・とびら） | ねこ・いぬ・ねずみ・ことり |
| そと | A `hideout` | 4（くさむら・いわ・きのほら・うえきばち） | かえる・りす・はりねずみ・ちょうちょ |
| うみ | A `hideout` | 4（いわ・すいめん・かいそう・つぼ） | クマノミ・たこ・かに・ぺんぎん |
| **のはら** | **B `chase`** | 4（くさむら×4） | **うさぎ 1匹**が移動して回る |

**4箇所より多くしない。** 1歳半の視野と指の届く範囲に4つ並べるのが上限で、
6つにすると1つあたりが小さくなり、押しにくくなる（みずのなかの切替ボタンで
88px×6 がスマホで折り返した実測がある。当たり判定も同じ問題を持つ）。

場面を増やすより、**同じ場所から違う動物が出る**（§6-2）ほうが効く。

**モードBの場面は、隠れ場所の種類を揃える。** のはらの4箇所を全部くさむらに
するのは手抜きではなく、**「どこに入ったか」だけに注意を向けさせる**ため。
形が4種類あると、移動先ではなく形のほうを見てしまう。

---

## 6. 飽きさせない仕組み

同じ場所を20回押しても続くように、**3段階**で変化をつける。

**モードB（§4-5）は、この仕組みをほとんど必要としない。**
行き先が毎回変わるので、変化が遊びの構造から自動的に出てくる。
下の仕組みは主にモードAのためのもので、モードBには 6-1 だけを効かせる。
モードBに 6-3 のサプライズを足すと、追うべき対象が増えて追えなくなる。

### 6-1. 毎回変わるもの（小）

- 登場の速さと大きさが ±10% ばらつく（同じ動きの繰り返しに見えない）
- 声のピッチが ±5% ばらつく
- 粒子の飛び方

### 6-2. ときどき変わるもの（中）

- **同じ隠れ場所から別の動物が出る。** `SpotConfig.animals` に複数入れておく。
  「またねこかな？」→「あ、うさぎだ！」の驚きが、繰り返しを支える
- 出かた（`style`）が変わる

### 6-3. たまに起きるもの（大・サプライズ）

- **2箇所から同時に出る**（10回に1回くらい）
- **とても大きい動物**が出る（ぞう・くじら。20回に1回）
- **モードAの場面でも、たまに移動する**（30回に1回）。
  これは §4-5 の移動をそのまま使う。**モードBを先に作れば、Aのサプライズは
  実装コストがほぼゼロ**になる（だから §12 では移動を Phase 3 に置いた）

サプライズの頻度は**低く保つ**。毎回起きると普通になり、驚きが消える。

---

## 7. 技術スタックと、水族館アプリからの再利用

### 7-1. スタック

TypeScript (strict) / three.js / Vite / vite-plugin-pwa

**React やその他の UI フレームワークは入れない**（60fps 確保とバンドルサイズのため）。
`any` を使わない。`as` によるごまかしも避ける。

### 7-2. そのまま持ってこられるもの（約2,000行）

「みずのなか」で作って**実機で検証済み**のもの。書き直さない。

| ファイル | 行数 | 何をするもの | 今回の役割 |
|---|---|---|---|
| `core/ScreenProjector.ts` | 50 | ワールド座標→画面座標の近接判定 | **必須**。当たり判定はこれ（§7-3） |
| `core/AudioBus.ts` | 816 | 音。素材が無ければ WebAudio で合成 | 「ばあ！」の声もこれで鳴らす |
| `core/AssetLoader.ts` | 647 | 素材の読込と手続き生成フォールバック | 不変条件7 の土台 |
| `core/WakeLock.ts` | 193 | 画面を消させない | そのまま |
| `core/QualityManager.ts` | 185 | fps を見て品質を5段階で上下 | そのまま |
| `core/Loop.ts` | 116 | 固定タイムステップの更新ループ | そのまま |
| `core/Input.ts` | 141 | タップ→3D座標、ジェスチャ封じ | **必須**。不変条件5 の実装 |
| `ui/ParentalGate.ts` | 164 | 右上24px・2秒長押し・3択確認 | そのまま |
| `ui/SettingsPanel.ts` | 283 | 音量・場面の表示制御 | 中身を差し替えて流用 |
| `feeding/Ripple.ts` | 99 | タップ地点の波紋（DOM） | 「押した実感」に流用 |
| `pwa/sw-register.ts` | 26 | Service Worker 登録 | そのまま |

`creatures/HideoutSystem.ts`（510行）と `creatures/ProceduralBottom.ts`（339行）は
**設計の下敷きとして読む**。ただし今回は主役なので、作り直したほうが良い。

### 7-3. 当たり判定は 3D のレイではなく画面座標で

**「みずのなか」で実際に失敗した点。**
3Dのレイ判定にすると、岩の裏や真横から当たりが出ず「押したのに反応しない」が起きた。
`ScreenProjector.distancePx()` で、**動物の位置を画面に投影してから距離で判定**する。
既定の当たり半径は 120px（指の大きさに対して十分）。

隠れ場所は4箇所しかないので、1つあたりの当たり半径を大きく取れる。
それでも**当たり判定どうしが重ならないこと**は E2E で検査する（§3-2）。
モードB（§4-5）では**空の場所も同じ当たり判定を持つ**。空だからと
判定を外すと、不変条件 3b（押したら必ず反応する）を破る。

### 7-4. 新規に書くもの

```
src/
├── peekaboo/
│   ├── SpotSystem.ts        隠れ場所の生成・状態遷移・当たり判定
│   ├── AnimalSystem.ts      動物の生成・登場アニメ・視線
│   ├── SpotShapes.ts        隠れ場所の手続き生成（箱・草むら・ドア…）
│   ├── ProceduralAnimals.ts 動物の手続き生成（モデルが無いとき）
│   ├── RevealEffect.ts      登場時の粒子
│   ├── ChaseSystem.ts       移動モード（§4-5）。行き先の抽選と、跳ねる移動
│   ├── EmptySpot.ts         空の場所を押したときの反応とヒント（§4-6）
│   └── Surprise.ts          §6-3 のサプライズ抽選
├── scene/
│   ├── SceneRoot.ts         1場面ぶんの生成と破棄
│   └── Background.ts        背景（実写 or 手続き生成）
└── data/scenes.ts           4場面の定義（データのみ）
```

---

## 8. Blender ワークフロー

Blender を使うのは**良い判断**。「みずのなか」で最後まで残った不合格5項目
（R1 縦の明度差 / R2a・R2b ハイライト / R3 輪郭 / R6 目）は、
**すべて素材が無いと直せないもの**だった。Blender はまさにそこを埋められる。

### 8-1. 何を Blender で作るか

| 優先 | 何を | なぜ |
|---|---|---|
| **1** | **目**（瞳＋ハイライト） | 判別の手がかりの第1位。1歳半は顔を見る。**目が無い動物は作らない** |
| **2** | 動物の .glb（11種） | シルエットで見分けられることが最重要 |
| 3 | 隠れ場所の .glb | 手続き生成でも足りるが、あると質が上がる |
| 4 | 体表テクスチャ | 縞・斑点。無いと単色に見える |

### 8-2. 「みずのなか」で踏んだ罠（必ず読むこと）

実際に時間を溶かしたものだけを書く。

**① UV が無いモデルにはテクスチャを貼れない**

`goldfish_ryukin.glb` は UV を持たず、`skinUrl` を `null` にせざるを得なかった。
用意した体表テクスチャ（182KB）は**1度も使われないまま**リポジトリに残っていた。

> **Blender: 必ず UV 展開してから書き出す（U → Smart UV Project でも可）**

**② 頭の向きと、体軸まわりの回転は自動判定できない**

`normalizeModelGeometry` は「いちばん長い軸が体軸」までしか決められない。
琉金は `modelRollDeg: 90` を入れないと**横倒しで泳いだ**。

> **Blender: 頭を −Y（glTF 書き出し後の −Z）へ、背を +Z（書き出し後の +Y）へ向けて、
> `Object → Apply → All Transforms` してから書き出す**

**③ 三角形数の上限を決めておく**

琉金モデル1体で **64,127三角形**あり、他の水槽（14,000〜30,000）の2〜4倍だった。
実機の 50fps は保てたが、素材の目安（1体 2,000三角形）の**32倍**。

> **Blender: 1体 2,000三角形まで。Decimate モディファイアで落とす。
> 同時に8体出るので、合計 16,000 が目安**

**④ メッシュを分けすぎない**

琉金は7枚の重なったメッシュ（ColorPatch / WhiteCheek / WhiteSaddle …）で、
**頭の縁が体を横切る弧として見えた**（realism-critic が判別の手がかりに挙げた）。

> **Blender: 書き出し前に `Ctrl+J` で1つのメッシュに統合する。
> 色分けは頂点色かテクスチャで行う**

**⑤ 目はジオメトリで作る。ハイライトも入れる**

R6 が不合格だったのは「目が無い種が3つ」あったから。
明るい楕円体を置くだけでは「コブ」にしか見えない。
**眼柄（短い柱）＋ 目玉 ＋ 瞳 ＋ 白いハイライト**の4点セットにする。

### 8-3. 書き出し設定（glTF 2.0 / .glb）

```
Format:          glTF Binary (.glb)
Include:         Selected Objects
Transform:       +Y Up  ✓
Data → Mesh:     Apply Modifiers ✓ / UVs ✓ / Normals ✓ / Vertex Colors ✓
Data → Material: Export（Principled BSDF のみ使う）
Animation:       いないいないばあ用のクリップだけ書き出す（§8-4）
Compression:     Draco は使わない（three 側でデコーダの読込が要り、起動が遅くなる）
```

### 8-4. アニメーションをどう作るか

「みずのなか」は**ボーンを使わず頂点シェーダ**で尾を振らせた（§6-5）。
理由は「群れで300匹動かす」前提だったから。

**今回は事情が違う。** 同時に見えるのは8体程度で、動きは「隠れる↔出る」の2ポーズ。
**Blender でアニメーションクリップを作って `AnimationMixer` で再生するほうが良い。**

- クリップは3つだけ: `hidden`（待機・もぞもぞ）/ `appear`（登場）/ `hide`（引っ込む）
- ボーンは**2〜4本**で足りる（体・頭・耳 or 尾）。多いと書き出しが重くなる
- シェイプキーでも良い（目を閉じる・口を開けるなど表情に向く）

**ただし、モデルが無くても動くこと**（不変条件7）。
手続き生成の動物は、コード側で同じ3状態を作る。

### 8-5. Blender が無くても進められる順番

Blender は学習コストがある。**先に手続き生成で全部動かしてから**、
1種ずつモデルに差し替えるのが安全。

1. 手続き生成だけで11種を作る（球と箱の組み合わせでもシルエットは出せる）
2. アプリとして完成させる
3. **いちばん出番の多い動物から** Blender に置き換える（ねこ・さかな）
4. 置き換えるたびに、**必ず実機で向きを確認する**（罠②）

---

## 9. 素材が無くても動く（不変条件7）

`public/` を空にしても `npm run dev` が動くこと。

| 素材 | 無いときの代替 |
|---|---|
| 動物の .glb | `ProceduralAnimals.ts` が球・箱・円錐で作る。目は必ず付ける |
| 隠れ場所の .glb | `SpotShapes.ts` が作る |
| 背景 | 単色グラデーション＋手続き生成の小物 |
| 「ばあ！」の声 | `scripts/make-voice.cjs` でオフライン合成した .ts を同梱（みずのなかと同じ方式） |
| 効果音 | WebAudio で合成 |

**声は SpeechSynthesis を使わない。** iOS のホーム画面アプリで、
例外も投げずに黙って無視されることがあり、実機で声が出なかった。
**音声データにして WebAudio で鳴らす**（みずのなかで確認済み）。

声は9本で 500KB ほどになったので、**`bakun` 相当の1〜2本以外は動的 import** にする。

---

## 10. パフォーマンス

### 10-1. 目標

| 指標 | 目標 |
|---|---|
| フレームレート | 中位のスマホで60fps（最低30fpsを下回らない） |
| 起動 | 3秒以内に何か表示 |
| メモリ | 場面切替で前の場面を完全破棄 |

### 10-2. fps は CI の合否にしない

開発環境にも CI にも GPU が無く、ソフトウェア描画の fps は実機性能を反映しない。
CI が見るのは「フレームが進んでいること」だけ。**実機での fps 確認は人間が行う。**

代わりに GPU に依存しない量で追う: draw call / 描画三角形 / CPU 更新時間 / テクスチャ推定量。

### 10-3. 毎フレーム走るコードで new をしない

動物・粒子はすべて `Float32Array` の使い回し。使い捨てのベクトルはモジュール定数に置く。

---

## 11. テストと受け入れ条件

### 11-1. 3層

| 層 | 何を見るか | 落ちたら |
|---|---|---|
| `tests/unit/safety.test.ts` | 不変条件のうち数値化できるもの | **実装を直す**（設計書より優先） |
| `tests/unit/*.test.ts` | 状態遷移・当たり判定・タイミング | 実装を直す |
| `tests/e2e/*.spec.ts` | ブラウザで壊れていないこと | 実装を直す |

### 11-2. CI が判定しないこと

- **fps の数値**（ソフトウェア描画のため）
- **スクリーンショットの画素比較**（動物が常に動くため必ず落ちる）
- **かわいいかどうか**（人間が実機で判断する）

### 11-3. 起動時間のテストは合否から外す

「みずのなか」で、同じコードが CI で赤にも緑にもなった（**11回中7回が赤**）。
このテストは全体の最初に走り、Chromium の初回起動と
SwiftShader の初回シェーダコンパイルを1件で払うため。
**テストは残し、値は毎回記録する。合否には数えない。**

### 11-4. E2E で必ず見ること

- どの隠れ場所を押しても、動物が `out` まで到達する
- **60Hz で連打しても登場が完走する**（貝の不具合の再発防止）
- 隠れているとき、画面に出ている面積が 0 でない（ヒントが見えている）
- **隠れ場所の当たり判定が、UI のボタンと重ならない**
- **隠れ場所どうしの当たり判定が重ならない**
- **モードBで、空の隠れ場所を押しても反応が返る**（不変条件 3b）。
  ふたが開き、そのあと正解の場所が揺れる
- **移動中に押しても反応が返り、かつ移動が完了する**（不変条件 4b）。
  60Hz で連打しても、うさぎは必ず次の隠れ場所に着く
- **ばあ→移動→隠れ終わりが 6秒以内に完了する**（§4-5 の想定は 4.8秒）
- **同じ隠れ場所に連続で戻らない**（20周まわして 0 回）
- 場面を10往復しても `renderer.info.memory` が増え続けない
- 素材がすべて404でも起動する
- 一度開けばオフラインで起動する

---

## 12. 実装フェーズ

各フェーズの末で必ず動作する状態にする。

### Phase 1: 骨組み（素材ゼロで動く）
- Vite + three.js + TypeScript strict
- `Loop` / `Input` / `Renderer` を「みずのなか」から移植
- 1場面・**隠れ場所4箇所**・手続き生成の動物3種
- **タップ → 出る → 引っ込む が動く**
- 受け入れ: `public/` が空でも動く

### Phase 2: いないいないばあの作り込み
- `SpotSystem` の状態遷移（§4-1）
- ヒント（§4-2）
- 登場のタイミングと演出（§4-3 / §4-4）
- **不変条件2 の連打テストをここで書く**
- 受け入れ: 60Hz 連打で登場が完走する

### Phase 3: 移動モード「おいかけっこ」
- `ChaseSystem`（§4-5）。行き先の抽選・予告・跳ねる移動・到着
- `EmptySpot`（§4-6）。空の場所の反応と、正解を知らせる揺れ
- **不変条件 3b / 4b のテストをここで書く**
- 受け入れ: 20周まわして、同じ場所への即戻りが 0 回。
  移動中に連打しても必ず到着する

**移動をここで作るのは、§6-3 のサプライズ（モードAでもたまに移動する）が
同じコードで済むから。** 後回しにすると、Aの移動を別実装してしまう

### Phase 4: 声と音
- `AudioBus` 移植
- 「ばあ！」の声を動物ごとに合成（`make-voice.cjs`）
- 初期ミュート・初回タップで解禁
- 受け入れ: 音素材が無くても鳴る

### Phase 5: 動物と隠れ場所を増やす
- **11種**（常駐4×3場面 ＋ 予備 ＋ 大物2。§5-2）× 隠れ場所の種類
- シルエットで見分けられること
- 受け入れ: 手続き生成だけで11種が別物に見える

### Phase 6: 場面と切替
- **4場面 × 各4箇所**（うち1場面はモードB）、切替バー
- **UI が当たり判定を塞がないことの E2E**（§3-2）
- 場面切替で dispose
- 受け入れ: 10往復してメモリが増えない

### Phase 7: 飽きさせない仕組み
- §6-1 / §6-2 / §6-3（移動サプライズは Phase 3 の `ChaseSystem` を呼ぶだけ）
- 受け入れ: 同じ場所を20回押して、出る動物が2種類以上

### Phase 8: 幼児対応の仕上げ
- ペアレンタルゲート・設定・WakeLock・PWA
- `prefers-reduced-motion`
- 受け入れ: §2 の不変条件を全項目チェック

### Phase 9: Blender で置き換え
- 出番の多い動物から .glb に差し替え
- **1種ごとに実機で向きを確認**（§8-2 罠②）
- 受け入れ: 三角形の合計が 16,000 以内

---

## 13. 公開

**GitHub は private、公開は Cloudflare Pages。**

「みずのなか」で確定した構成。理由:
- 無料プランの GitHub Pages は public リポジトリでしか使えない
  （private にするとサイトが止まる）
- Pages の URL は必ず `<ユーザー名>.github.io/...` で、個人名が入る
- **Netlify の無料プランは「Powered by Netlify」のバッジが出て、
  画面下部の UI に重なる**（実際に重なって、ボタンが押せなくなった）
- Cloudflare Pages は無料・バッジなし・private 対応・`<好きな名前>.pages.dev`

`BASE_PATH` は指定しない（サイト直下で配信するので vite の `base` は `/`）。
Service Worker と `index.html` は `must-revalidate` にする
（長期キャッシュにすると、見る人の端末が古い版のまま更新されない）。

---

## 14. 「みずのなか」から引き継ぐ教訓（実測値つき）

新しく入る人が同じ失敗をしないように、**測った数字ごと**残す。

### 14-1. 「連打しても壊れない」は「連打しても動く」まで確かめる

貝は開閉の途中でタップされると向きを反転していたため、連打すると
**0.45秒の開閉が一度も完了せず、開き量の最大が 0.037**（1回押しなら 1.0）だった。
例外を返して何もしないのは、無反応を作るのと同じ。

### 14-2. 当たり判定は画面座標で

3Dのレイだと岩の裏や真横から当たりが出ない。`ScreenProjector` を使う。

### 14-3. 当たり判定どうしの距離を測る

貝と岩が画面上 **21px** しか離れておらず、岩が先に取っていた。
配置を決めたら、必ず画面座標での距離を数値で確認する。

### 14-4. 「大きくすれば見える」は成り立たないことがある

生き物を2.5倍にしても、視認性の指標（24枚の最小値）は **0.016% から動かなかった**。
葉の隙間から見える細切れの連結成分が増えるだけだから。
**指標が測っているものを理解してから、対策を打つこと。**

### 14-5. オブジェクトを1つ足すと、無関係な測定値まで動く

three は `generateUUID()` で1オブジェクトにつき `Math.random()` を4回消費する。
モデルを1つ足しただけで、触っていない水槽のスコアが 0.980 → 0.173 に動いた。
**遊びの乱数は `makeRandom()` の独立したシードに分ける。**

### 14-6. 決定論的なスクリーンショットには6つ全部が要る

①Math.random を固定シードに ②ループを止めて手動で進める ③動画を同じ時刻で止める
④カメラの微動を切る ⑤品質を固定 ⑥グレインの時刻を毎回0に戻す。
**どれか1つでも欠けると絵が毎回変わり、baseline との比較が撮り直しの揺らぎになる**
（実測: ループを止め忘れていたとき 12枚中11枚が不一致）。

### 14-7. 加算で足す光は、体色を消す

体色は「テクスチャ × 光」だが、コースティクス・リム・光条は**加算**。
彩度 0.70 のクマノミが淡いサーモンピンクにしか見えなかった原因は、
生き物に乗せたコースティクス（最大 +1.7）だった。
**「色が出ない」ときは、素材より先に加算項の強さを疑う。**

### 14-8. 素材のURLは必ず `resolveAssetUrl()` を通す

`/models/...` のような絶対パスのままだと、サブパス配信で素材を置いた瞬間に404になる。
素材が無いあいだはフォールバックが効いて「一応動く」ので気づけない。

---

## 15. まだ決めていないこと

実装を始める前に決めたほうが良いもの。

| # | 論点 | 選択肢 | 備考 |
|---|---|---|---|
| 1 | 見た目の方向 | ①3Dのまま ②2Dイラスト風にトゥーンシェード | 1歳半には②のほうが見やすいかもしれない。Blender はどちらでも作れる |
| 2 | 背景 | ①手続き生成 ②実写写真 ③Blender でレンダリングした1枚絵 | ③は「みずのなか」で使わなかった手。質と手間のバランスが良い可能性 |
| 3 | モードBの数 | 4場面のうち1つか、2つか | まず1つで様子を見る。移動を気に入るなら増やす（§4-5） |
| 3b | 移動の速さ | ぴょんぴょん 3〜4回（約1.5秒）で足りるか | 遅いと待てない、速いと追えない。**実機で子どもの視線を見て決める**唯一の数値 |
| 4 | 「ばあ！」の声 | ①合成音声 ②保護者の声を録音 | ②は圧倒的に効く可能性がある（親の声に反応する月齢）。ただし録音の仕組みが要る |
| 5 | リポジトリ | ①新規 ②`suizokukan` のモノレポ化 | ①を推奨（不変条件や CI が別物になるため） |

**特に 4 は検討の価値がある。** 保護者が「ばあ！」を1回録音して使えるようにすると、
合成音声とは比べものにならない反応が期待できる。
ただし録音UIは保護者ゲートの内側に置く必要がある（§2 の入力欄の禁止と両立させる）。

---

## 16. 最初の一歩

この設計書を新しいリポジトリに置いて、Claude Code に次を渡す。

> `peekaboo-app-design.md` を読んで、Phase 1 を実装してください。
> 素材は1つもありません。手続き生成だけで、
> 「隠れ場所を押すと動物が出て、少ししたら引っ込む」が動く状態にしてください。
> §2 の不変条件は最優先です。特に不変条件2（どの瞬間に押しても反応する）は、
> 実装と同時に `tests/unit/safety.test.ts` で数値のテストにしてください。

Phase 1 が動いたら、**次は Phase 3（移動モード）を早めに触ること**。
このアプリでいちばん効くのは移動を見せることで（§1）、
それが本当に効くかは実機で子どもに見せないと分からない。
動物を増やす（Phase 5）より先に、**移動が面白いかを確かめる**。
