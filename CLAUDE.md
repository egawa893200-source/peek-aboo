# CLAUDE.md — ばあ！

## このプロジェクト

1歳半の子どもがタップして遊ぶ**いないいないばあ**アプリ。
隠れ場所を押すと動物が出て「ばあ！」と言い、少ししたらまた隠れる。それだけ。

**見て楽しい／触ると必ず反応する**がすべてで、機能追加より「壊れないこと」と
「無反応を作らないこと」が優先される。

詳細仕様は `peekaboo-app-design.md`（設計書）。節番号（§4-5 など）はそこを指す。
判断に迷ったら設計書を読むこと。

**姉妹プロジェクト: 水族館アプリ「みずのなか」。**
`src/core/` と `src/ui/` の大半はそこからの移植で、実機で検証済み。
下の「みずのなかで実測して分かったこと」は、同じ失敗を繰り返さないための記録。

## 技術スタック

TypeScript (strict) / three.js / Vite / vite-plugin-pwa

### 禁止事項

- **React・その他UIフレームワークを入れない**（60fps 確保とバンドルサイズのため）
- `any` を使わない。`as` によるごまかしも避ける
- 外部リンク・広告・課金・テキスト入力欄を子ども向け画面に置かない
- 新しい依存を安易に増やさない。手続き生成で済むものは自前で作る
- **合格ラインを自分で緩めない。判定基準を書き換えない**（人間が決める）
- **テストを削除したり期待値を緩めて通さない。実装を直す**

## 絶対に壊してはいけない不変条件

設計書 §2 が正。**ここだけは実装より優先する**（食い違ったら実装を直す）。

1. **画面のどこをタップしても必ず何かが起きる。** 無反応を作らない
2. **どの瞬間に押しても反応する。** 出ている途中・引っ込む途中でも受け付ける。
   「アニメーション中だから無視」は禁止
3. **隠れている動物は必ず体の一部が見えている**（移動モードで空になった場所は例外）
3b. **空の隠れ場所を押しても必ず反応する。** そのあと正解の場所を揺らして知らせる
4. **登場は 0.35秒以内に始まる**
4b. **移動中に押しても反応する。** ただし移動そのものは止めない
5. **スワイプ・ピンチ・ダブルタップ・長押しに機能を割り当てない**
   （例外はペアレンタルゲートの2秒長押しのみ。画面右上 24×24px）
6. **1秒に3回を超える明滅を作らない。** 急激な全画面フラッシュ禁止。
   `prefers-reduced-motion` 指定時は光の演出を弱める
7. **素材ファイルが1つも無くてもアプリは必ず起動する。**
   `public/` を空にしても `npm run dev` が動くこと
8. **場面切替時に前のシーンの geometry / material / texture を dispose する**
9. **音は初期ミュート。** 最初のタップで解禁し、フェードインさせる
10. **自動マージしない。** マージ判断は常に人間が行う。
    CI から Claude を自動で動かす仕組みも入れない（従量課金になるため）

### 不変条件は数値のテストで押さえる

「壊れても静かに壊れる」ものは目で見て気づけないので `tests/unit/safety.test.ts` に置く。
**このファイルが落ちたら、設計書ではなく実装を直すこと。**

## ディレクトリ構成と責務

```
src/
├── main.ts            エントリ。DOM要素を集めて App に渡すだけ
├── types.ts           SceneConfig / SpotConfig / AnimalConfig などの型
├── data/
│   ├── scenes.ts      4場面の定義（データのみ。ロジックを置かない）
│   └── animals.ts     動物の定義（同上）
├── app/App.ts         全体制御・場面切替・各システムの配線
├── core/              ★ みずのなかからの移植（実機で検証済み。書き直さない）
│   ├── Renderer.ts        WebGLRenderer（ポスト処理は入れていない）
│   ├── Loop.ts            固定タイムステップ(1/60・最大3サブステップ)
│   ├── AssetLoader.ts     素材の読込と、無いときの手続き生成フォールバック
│   ├── QualityManager.ts  fps を見て品質を5段階で上下
│   ├── Input.ts           タップ→3D座標。ブラウザの既定ジェスチャ封じ
│   ├── ScreenProjector.ts ワールド座標→画面座標の近接判定（当たり判定はこれ）
│   ├── AudioBus.ts        音。素材が無ければ WebAudio で合成
│   └── WakeLock.ts        画面を消させない
├── peekaboo/          ★ このアプリの心臓部（新規に書く）
│   ├── SpotSystem.ts        隠れ場所の生成・状態遷移・当たり判定
│   ├── AnimalSystem.ts      動物の生成・登場アニメ・視線
│   ├── ChaseSystem.ts       移動モード（§4-5）。行き先の抽選と跳ねる移動
│   ├── EmptySpot.ts         空の場所を押したときの反応とヒント（§4-6）
│   ├── SpotShuffle.ts       同じ場所から別の動物が出る（§6-2）
│   ├── SpotShapes.ts        隠れ場所の手続き生成（箱・草むら・ドア…＋たまご）
│   ├── ProceduralAnimals.ts 動物の手続き生成（モデルが無いとき）
│   ├── CutoutAnimal.ts      参照画像を板に貼る（道A。既定はこちら）
│   ├── RevealEffect.ts      登場時の粒子
│   └── Surprise.ts          §6-3 のサプライズ抽選
├── scene/
│   ├── SceneRoot.ts   1場面ぶんの生成と破棄
│   ├── Backdrop.ts    背景のグラデーションと接地影（道D）
│   ├── Flavor.ts      場面ごとの味つけ（§6 のスペシャル。データで選ぶ）
│   └── Background.ts  背景（静止画 or 手続き生成）
├── ui/
│   ├── ParentalGate.ts  ★ 移植。右上24px・2秒長押し・3択確認
│   ├── Ripple.ts        ★ 移植。タップ地点の波紋
│   └── styles.css       ★ 移植。ブラウザの既定ジェスチャ封じは触らない
└── pwa/sw-register.ts   ★ 移植
```

★ = 「みずのなか」からの移植。**ファイル冒頭に出どころを書いてある。**
実機で検証済みなので、理由なく書き直さないこと。

## コーディング規約

- TypeScript strict。`noUnusedLocals` / `noUnusedParameters` も有効
- **three.js のリソースは必ず dispose する**。geometry / material / texture。
  場面を捨てるときに1つでも漏らすとリークする
- **毎フレーム走るコードで new をしない**。使い捨てのベクトルはモジュール定数に置く
- **素材のURLは必ず `resolveAssetUrl()`（`core/AssetLoader.ts`）を通す**。
  素材が無いあいだはフォールバックが効いて「一応動く」ので、
  通っていないことに気づけない
- **音の大きさは素材側で作る。`master` の手前で 1 を超えるゲインを掛けない**。
  `master` のゲインは音量スライダーそのもの
- 例外を投げてユーザーに見せない。素材が読めない・APIが無い等はすべて
  黙ってフォールバックする（エラー画面を出さない）
- コメントは「なぜそうしたか」を日本語で書く。特に、試して駄目だった案や
  数値を選んだ理由は必ず残す（同じ失敗を繰り返さないため）

## テストも型検査の対象

`npm run typecheck` は `tsconfig.json`（src のみ）に加えて
`tsconfig.test.json`（src + tests）も走らせる。
**テストから src の型を参照するときは、書き写さず import すること。**
みずのなかでは、E2E が `window.__aquarium` の型を書き写していたせいで、
後から足した API が「存在しないプロパティ」のまま残っていた。

## 開発コマンド

```bash
npm run dev          # 開発サーバ（Service Worker は無効）
npm run build        # 型チェック + 本番ビルド
npm run typecheck    # 型チェックのみ
npm run test         # Vitest（ロジックの単体テスト）
npm run test:e2e     # Playwright（ブラウザ実動作）
npm run verify       # typecheck + test + test:e2e
npm run cutouts      # reference/animals/<id>/front.png → public/animals/<id>.webp
npm run backgrounds  # reference/backgrounds/<id>.png  → public/backgrounds/<id>.webp
```

## CI が判定しないこと

- **fps の数値**。開発コンテナにも CI にも GPU が無くソフトウェア描画なので、
  実機性能を反映しない。「フレームが進んでいること」だけを見る
- **スクリーンショットの画素比較**。動物も背景も常に動くので必ず落ちる。
  **例外が1つある**（2026-09-06、人間が決めた）。`npm run shot -- --pose reference`
  が撮る**静止1枚**（無背景・シード固定・アニメーションなし・平行投影）に限り、
  画素比較を合否条件にしてよい。上の禁止の理由「常に動くので必ず落ちる」が
  静止ポーズには当たらないため。ただし**当たらないことを毎回証明する**こと:
  同じコードで2回撮ってバイト単位で一致しなければ使わない。
  詳細は `docs/match-gate.md`。**動いている場面の比較は従来どおり禁止。**
- **見た目が良いかどうか**。これは人間が実機で判断する

## みずのなかで実測して分かったこと

**すべて実測値。同じ失敗を繰り返さないために残してある。**

### 「連打しても壊れない」は「連打しても動く」まで確かめる

貝は開閉の途中でタップされると向きを反転していたため、連打すると
0.45秒の開閉が一度も完了せず、**開き量の最大が 0.037**（1回押しなら 1.0）だった。
実機で「触っても反応しない」と報告された。
開閉中のタップは**受け取るが向きは変えない**ようにして直した。

**例外を返して何もしないのは、無反応を作るのと同じ。**
不変条件2 はこれが理由で、テストは「連打しても登場が完走する」まで見る。

### 当たり判定は 3D のレイではなく画面座標で

岩の裏や真横から当たりが出ず「押したのに反応しない」が起きた。
`ScreenProjector.distancePx()`（ワールド座標→画面座標の距離）で判定する。
既定の当たり半径は 120px。

### 当たり判定の半径は、定数では決められない（このプロジェクトの実測）

設計書 §7-3 の既定 120px を 2×2 の4箇所に当てると、**どの画面でも重なる**。

| 端末 | いちばん近い組 | 120+120 に対して |
|---|---|---|
| Pixel 7 412×839 | 204.8px | 35.2px 食い込む |
| iPhone 12 390×750 | 183.0px | 57.0px 食い込む |
| 360×600 | 146.4px | 93.6px 食い込む |
| 844×390（横持ち） | 118.7px | 121.3px 食い込む |

ワールド座標を固定したまま画面の大きさだけ変わるので、**どんな定数を選んでも
全端末では成立しない**。`SpotConfig.hitRadiusPx` は上限として持ち、
`SpotSystem.radiusAt()` が隣との距離を見て縮める。
**下限は設けていない。** 下限が効いた瞬間に円が重なって、
「押したのに隣が反応する」（みずのなかの貝と岩）が復活するため。

### 隠れ場所を傾けると、体が見える（このプロジェクトの実測）

場面ごとの演出で「風でゆっくり揺れる」を隠れ場所そのものに掛けたら、
**うみ の かいそう は 0.75° で体が見えた**（15×15 の格子のうち1点）。
傾けられる角度を測ると、場所によって桁が違う:

| 場面 | いちばん弱い場所 | 角度 |
|---|---|---|
| うみ | かいそう | **0.75°** |
| どうぶつえん | うえきばち | 6.25° |
| おうち | ふとん | 7.25° |
| そと・のうじょう・きょうりゅう・のはら | — | 12°でも漏れなし |

かいそうに合わせると 0.3° しか振れず、画面では 1px も動かない。
**見えない演出のために不変条件3 の余裕を食うのは割に合わない。**
だから常時のゆれは、隠れ場所より奥（z < 0）に置いた飾りだけを揺らす。
**奥にあるので、動物を隠すことが原理的にない。**
短く回す演出（ため のもぞもぞ 1.7°・地ひびき 2.9°）だけ隠れ場所に掛けてあり、
**その2倍まで体が覗けないことをテストが場面ごとに見張っている。**

### ふたを開ける演出は、中身が居る隠れ場所には使えない（このプロジェクトの実測）

「みんなで鳴く」を `extraOpen`（ふたを開ける）で作ったら、体が見えた。
開けられる量（15×15 の格子で漏れが出ない上限）は場所によって桁が違う:

| 場面 | いちばん弱い場所 | 開けられる量 |
|---|---|---|
| どうぶつえん | たかき | **0.05** |
| おうち | カーテン | **0.05** |
| うみ | かいそう | 0.10 |
| のうじょう | わら | 0.10 |
| そと | きのほら | 0.20 |
| のはら | くさむら | 0.20 |

いちばん弱い場所に合わせると 0.05 で、画面ではまったく動いて見えない。
**ふたを開ける演出は、空の隠れ場所にだけ許される**（§4-6 の空振りと
§4-5 の到着は、どちらも中身が居ない場所）。中身が居る場所には
`callShake`（揺れ）を使うこと。

### 隠れ場所の大きさが、そのまま動物の大きさの上限（このプロジェクトの実測）

「動物が小さくなった。特にキリンやゾウ」と言われて測ったら、
**上の隠れ場所の天井ではなく、隠れ場所そのものの大きさ**で頭打ちだった。

| 動物 | 効いていた制約 |
|---|---|
| きりん | 高さ（`room`）。幅には 3倍の余裕があった |
| ぞう | 幅（`mouthWidth × 0.86`） |

隠れ場所を大きくした（W 1.35 → 1.70 / H 1.05 → 1.35）。
**上げすぎると上の段が画面の外に出る**（+2.45 で ぞう の頭が切れた）。
`SCREEN_TOP_Y` は `0.3 + tan(33°) × 7.2 = 5.0` ではなく **4.3**。
カメラは原点を見ているので 2.4° 下を向いていて、そのぶん上が狭い。

上下の段で見えている高さがそろうところを実測で選んだ（+1.90 / −2.00）。

### 縁から出る量は、体の大きさで割ってそろえる（このプロジェクトの実測）

見えている量は「ヒントの高さ × 倍率 − 沈めた深さ」。沈める深さは絶対値
（見下ろす角度で決まる）なので、そのままだと**小さい動物ほどはみ出しが減る**。
隠れ場所を大きくしたら、大きい絵 0.225 / 小さい手続き生成 0.127 と、
§4-2 の band（0.15〜0.25、幅 0.10）を使い切ってしまった。
**沈めたぶんをヒント側で伸ばして打ち消す**と、大きさによらず一定になる
（実測 0.212〜0.235）。

### 「縁の高さ」と「縁に見える高さ」は違う（このプロジェクトの実測）

いわ の庇は上端を `coverTopY` ちょうどに置いてあった。上の段は
カメラより上にあるので**見上げる形**になり、手前（z = 0.20）にある板の
上端は縁より高く見えて、出てきた体の下半分を隠した（見えている割合 53%）。
下の段は逆で、上端が縁より低く見えて隠れている体が覗く。
**上下で符号が変わる**ので、隠れ場所の高さから計算する（`browShift`）。

### 出きった動物は、上の隠れ場所に覆いかぶさる（このプロジェクトの実測）

出きった動物は縁より `height × OUT_LIFT` 上に出る。絵を隠れ場所いっぱいに
合わせた結果、**下の段の動物が上の段の隠れ場所に食い込んでいた**。

| 場面 | 重なり |
|---|---|
| どうぶつえん | さる → きげあーす 0.92 × 0.39 |
| きょうりゅう | きかぶ → おおいわ 1.04 × 0.45 |
| うみ | つぼ → かいそう 0.95 × 0.31 |
| そと | ほら → くさ 1.19 × 0.05 |
| のうじょう | おけご → わら 0.73 × 0.19 |

上限は**上の隠れ場所の実際の下端**で決める（`coverBottomY` の宣言値では
背板や鉢のぶんが抜けて、重なりが残った）。§6-1 のばらつき（最大1.1倍）と
出たあとの癖（最大 +0.12）を見込んで `OUT_TOP_SLACK` = 1.3 を掛ける。

**そのままだと動物が小さくなりすぎる**（下の段が 61〜97px）。
隠れ場所を上下に広げて（±1.55/−1.20 → +2.05/−1.75）73〜146px に戻した。
**これ以上広げても 1〜3px しか増えない**（そこから先は隠れ場所そのものの
大きさが上限）。たまごに入れ替わると下端が変わるので、
**入れ替えたら全部を置き直す**（`reanchorAll`）。

### 隠しているつもりが、絵では隠れていない（このプロジェクトの実測）

**隠れているときの遮蔽テストを、手続き生成の動物だけで回していた。**
実機で見たら、うみ の すいめん で**クマノミが丸ごと画面に出ていた**。
絵を貼った動物（道A）は板1枚で、前板との隙間を埋めるために
`FORWARD`（0.26）だけ手前に出してある。水面や葉のように薄い覆いは、
その手前に出た板を隠せない。手続き生成の体（厚み 0.4）では届いていなかった。

直したこと:
- 板は**隠れているあいだ奥に置く**（`setDepth(reveal)`）。出るにつれて手前へ
- 遮蔽テストを**絵ありでも回す**（`sceneRig(id, seed, useCutouts)`）
- `FIT_MARGIN` を 0.02 → 0.10（こや と しだ の下から足が出ていた）

**板は四角いので、レイは透明な角にも当たる。**
それを「見えている」と数えると、実際には見えていないのに落ちる
（のうじょうの こや で下端 7点）。`npm run cutouts` が 24×24 の粗い
アルファ（`CUTOUT_MASKS`）も書き出すようにして、テストは
**絵のあるところだけ**を数える。1画素でも不透明なら 1 にしてあるので、
見えているものを見逃す側には倒れない。

### ヒントに顔を使わない（このプロジェクトの実測）

絵の上側 27% を切って縁から出していたら、**顔が丸ごと見えて誰か分かった**。
「ばあ」は開けるまで分からないから面白いので、縁から出すのは
§5-1 の `hintPart`（耳・尻尾・ひれ）だけにする。
絵と手続き生成が混ざるが、顔が見えるよりはるかにまし。

### 「見えない」ときは、赤く塗って撮る（このプロジェクトの実測）

うさぎの足あとを跳ねた高さに落としたら、**4つ置いても画面に1つも無かった**。
数（`getFlavor().footprints`）は 4 と出ているので、抜けてはいない。
色を赤・大きくして撮ったら、5つのくさむらのどれかが必ず前に来て
隠していると分かった。**「置いたのに見えない」は、まず遮蔽を疑う。**
足あとはくさむらの下端より下（y = -2.6）に横一列で残すことにした。
どこを通ったかは左右で読めるので、高さは揃っていてよい。

### 促音「っ」は、綴りに閉鎖音を入れないと出ない（このプロジェクトの実測）

`kochi kochii` は実機で「こち こち」に聞こえた。促音は子音の閉鎖なので、
音として何も無いと消える。スペイン語の 'ch' は /tʃ/ の二重字なので、
**`kotchi` と書くと直前の 't' が独立した /t/ になり、そのまま「こっち」**になる。
実測（10ms ごとの山）: `kochi` は「こ」のあと 40ms の谷しか無いが、
`kotchi` は 60ms ＋ 70ms の谷が2つ入る。

### 画面に入る範囲は、思っているより狭い（このプロジェクトの実測）

縦持ちは fov 66°・アスペクト 0.49。奥行き z = -0.75（カメラから 7.95）で
画面に入るのは **y が ±5.16、x は ±2.53 しかない**。
飾りを x = ±5.2 に並べたら 9株のうち4株が画面の外で、
三角形が 27 増えるはずのところ 9 しか増えなかった。
**「置いたのに増えない」ときは、まず画面に入っているかを疑う。**

### 「見えていない」は、位置ではなく遮蔽で確かめる（このプロジェクトの実測）

隠れているときのはみ出し量を高さで測っていたが、**高さが正しくても見えていない**
ことが3回あった。どれも数値のテストは通っていた。

| 何が起きたか | 数値では |
|---|---|
| ねこの尻尾が、閉じたふたの裏で1画素も見えない | はみ出し 0.22 で正常 |
| カーテンの中央 0.10 の隙間から体が丸見え | はみ出し 0.22 で正常 |
| うさぎの体が、くさむらの下から飛び出す | 上側のはみ出しは正常 |

画素比較は禁止（動物が常に動く）なので、**カメラからレイを飛ばして遮蔽を見る**。

- ヒント → 途中に何も無いこと（見えていること）
- 体 → 見かけの矩形に **15×15 の格子**を張って、いちばん手前が体でないこと

**格子は隙間の幅より細かく取ること。** 7×9 では 0.05 幅の隙間をすり抜けた。
カメラは斜めから見ているので、体の座標と隠れ場所の座標はずれる。

体は縁より `HIDDEN_SINK`（0.06）沈める。ちょうど同じ高さに置くと、
見下ろす角度（11.8°）で頭が覗く。

### 動物を増やすなら、`id` の分岐ではなく形の記述にする

5体のうちは `switch (cfg.id)` で足りていたが、17体にした時点で
分岐が「顔」「頭の上」「尾」の3箇所に散り、**新しい動物を足したときに
1箇所だけ入れ忘れる**形になった（実際に既存5体の耳と尾が消えた）。

`AnimalConfig` に `bodyPlan` / `headTop` / `snout` / `tail` / `coat` を持たせて、
**データを読めば輪郭が分かる**ようにした。`ProceduralAnimals` に
新しい動物の分岐を書くことは、もう無い。

### 体の底は、作り手ではなく生成側で y=0 に揃える

「原点は足元」を約束にして各 build 関数に守らせていたが、
かに（+0.082）・さかな（+0.046）・たこ（-0.027）でずれていた。
ずれたぶんだけ隠れ場所の縁から頭が出る（かにで実測 0.043）。
組み上がったあとに境界箱を測って**機械的に揃える**。

### 部品は自分の枠（±1.375）から出さない

隠れ場所は 2.75 おきに並んでいる。すいめんの水を下へ動かしたら、
**下の段の隠れ場所まで水柱が伸びて、そこに居るかにを丸ごと覆っていた**
（水柱が世界座標 y = -0.71 まで届き、かには -0.72〜-0.07）。
開く動きは、平行移動ではなく**縮める／回す**にすると枠から出にくい。

### 縦に開く隠れ場所は成り立たない

きのほらを上下の唇が開く形にしたら、上の唇が上がりきっても
出てきた動物（局所 0.46〜1.10）に重なって、**はりねずみが一度も見えなかった**。
上へ逃がすには枠を越える必要がある。動物は上に出てくるので、
**ふたは横に逃がすか、手前に倒すかのどちらか**。

### 球だけで隠れ場所を作ると、必ず隙間ができる

球は上下で横幅がすぼまるので、球を並べた「開く半分」は
中央や外側に菱形の隙間を残す。いわ・うえきばち・くさむらで全部起きた。
**平らな板を1枚重ねて塞ぐ**（開いても動物を隠さない高さに置く）。
格子 15×15 のレイのテストが、どれも数点〜数十点で捕まえた。

### 「声が鳴らない」は、たいてい間隔制限

`AudioBus.speak()` は読み上げ用の入口で、最短間隔 1.25秒 が掛かっている。
登場の「ばあっ！」をこれで鳴らすと、続けて別の隠れ場所を押したときに
**声が落ちる回**ができる。録音した1本を鳴らすだけの `playVoice()` を通すこと
（重なりは `voiceBusyUntil` が防ぐ）。

`speak('あれ？', 'baa')` と書くと**録音してある「ばあっ！」が鳴る**。
第1引数の文字は読み上げが使えるときにしか使われない。
空振りなのに「ばあっ！」と言うのは、いちばん紛らわしい間違いだった。

### タイミングの合否は壁時計で測らない

`Loop` は 1/60 の固定刻みで、1フレームに最大3ステップしか進めず、
溜まりすぎたぶんは捨てる。だから **fps が足りない環境では実時間より遅れる**。

§4-5 の「1周 4.80秒」を壁時計で測ると、CI のソフトウェア描画では 5.7〜7.0秒。
そのまま合否にすると、fps を合否条件にしたのと同じことになる（§10-2 に反する）。

**「待つ長さ」も壁時計で決めないこと**（2026-09-06 に踏んだ）。
連打しながら1周を待つ e2e が「壁時計で 5000ms」待っていた。
GPU の無い環境では 13fps しか出ず、`Loop` は1フレームに最大3ステップなので、
5秒待っても更新時計は 3.0秒ぶんしか進まない。1周に届くかどうかが
**その日の速さ次第**になり、同じコードが緑にも赤にもなった。
壁時計は「固まったときに止める」ためだけに使う。
`__peekaboo.getSimulatedSeconds()`（＝`Loop.simulatedSeconds`）で測ること。
更新時計なら 4.83〜4.87秒で、表と1フレーム以内で一致する。

### 当たり判定どうしの距離を測る

カニとエビが両方 y = -3.34 の 0.16m 差で置かれ、実機で重なって見えた。
**置いたら距離を測ること。** 目で見て気づけない。

### 「大きくすれば見える」は成り立たないことがある

生き物を 2.5倍にしても、判定に使っていた指標（フレーム内で最小の連結成分）は
0.016% から**1ミリも動かなかった**。大きくすると、岩や海藻の隙間から見える
細切れの数が増えるだけだったため。
**指標が動かないときは、対象ではなく指標の設計を疑う。**

### オブジェクトを1つ足すと、無関係な測定値まで動く

three は `generateUUID()` で1オブジェクトにつき `Math.random()` を4回消費する。
モデルを1つ足しただけで乱数列がずれ、**触っていない水槽の測定値**が
0.980 → 0.173 に動いた。
遊びの乱数は、共有のシードではなく独立したシードから引くこと。

### `Input` は `document.body` に付ける

`#overlay-layer` は `pointer-events: none`（子要素で個別に有効化する）なので、
そこに付けるとタップが**1度もハンドラに届かない**。
この骨組みを作ったときに実際にやって、画面のどこを押しても
`getTapCount()` が 0 のままだった（不変条件1 が丸ごと死ぬ）。
E2E の「どこを押しても波紋が出る」がこれを捕まえた。

### 右上 24×24px だけは遊びの当たり判定を持たない

ペアレンタルゲートのホットスポット（不変条件5 の唯一の例外）。
ここを押してもタップは通らない。
**「無反応のバグ」と読み違えないこと。** E2E の隅のタップは右上だけ
40px 内側に寄せてある。

### UI が当たり判定を塞ぐ事故

切替ボタンを 88px × 5個 並べたら、390px 幅の端末で2行に折り返し、
バーの高さが 172px になって画面下部の岩に覆いかぶさった。
押しても岩が反応せず、水槽の切替になっていた。
**画面下部に UI を置いたら、その下にある当たり判定と重なっていないか E2E で見る。**

Netlify 無料プランの「Powered by Netlify」バッジも同じ事故を起こした
（右下に固定表示され、いちばん右のボタンを塞ぐ）。
`styles.css` の `--badge-reserve` で逃がしてある。

### 加算で足す光は、体色を消す

体色は「テクスチャ × 光」だが、リムライトや光の効果は**加算**なので、
体色がいくら鮮やかでも上から白を足せば消える。
彩度 0.70 のクマノミが淡いサーモンピンクにしか見えなかった原因は、
生き物に乗せていた加算の光（最大 +1.7）だった。
**「色が出ない」ときは、素材や `color` を触る前に加算項の強さを確かめる。**

### トーンマッピングは Neutral

ACESFilmic は映画向けのカーブなので、彩度の高い体色を大きく退色させる。
**動物を見分けるのは色と輪郭**なので、ここは効く。

### `antialias` はポスト処理を通すと効かない

`WebGLRenderer({ antialias })` はキャンバスにしか効かない。
`EffectComposer` を挟むとシーンはレンダーターゲットに描かれるので、
composer 側に `multisampling` を渡さないと、どの端末でもジャギーのままになる。
輪郭が1画素で切り替わって「切り絵」に見えていた直接の原因。

### モデルを追加したら必ず動かして向きを見る

頭が -z のモデルは `modelFlip: true` が要る（**自動判定は誤るのでやめた**）。
さらに**体軸まわりの向き**も合っているとは限らない。背が上を向くかは
`modelRollDeg` で明示する（琉金は 90 が要り、入れないと横倒しで泳いだ）。

### 形を指定しないと、全部同じ形になる

体型（`bodyHeight` / `bodyWidth`）を指定しないと、チョウチョウウオもメダカも
同じ魚になった。**見分けがつくかは色より輪郭で決まる。**

### 声は読み上げに頼らない

`SpeechSynthesis` は iOS のホーム画面アプリで**例外も投げずに黙って無視される**
ことがあり、実機で声が出なかった。効果音は WebAudio で確実に鳴っていたので、
声も音声ファイルにして同じ経路で鳴らすようにした。
`scripts/make-voice.cjs` が WAV を base64 の .ts に変換する。
**声を足すときは動的 import にすること**（静的に3本足したら初期バンドルが
847KB → 1,023KB になった）。

## 背景の絵（2026-09-07。7場面ぶん入った）

**元の絵は `reference/backgrounds/<場面id>.png`**（1024 × 2048 の PNG）。
`npm run backgrounds` が `public/backgrounds/<場面id>.webp` に変換し、
`SceneConfig.backgroundUrl` がそれを指す。**置かなければ従来どおり
手続き生成のグラデーション**（不変条件7）。

**PNG のまま `public/` に置かないこと。** 7枚で 9.0MB あり、
WebP（品質 0.82）にすると **181KB** になる。50分の1。
変換はヘッドレス Chromium にやらせている（WebP エンコーダを依存に足さないため。
`scripts/make-cutouts.mjs` と同じ作り）。

- **推奨 1024 × 2048（縦 1:2）。** この奥行き（z = -1.55、カメラから 8.75）で
  画面に入るのは縦 11.36・横 5.57 なので、1:2 がちょうど収まる
- **縦横比は崩さない。** グラデーションと同じ 16×16 の板に貼ると横に潰れるので、
  絵は別の板（`createBackdropImage`）に、絵の比のまま貼る
- **光を当てない**（`MeshBasicMaterial` ＋ `toneMapped = false`）。
  描いたとおりの色を出すため。動物の絵（道A）と同じ理由
- うしろにグラデーションの板が残るので、**横持ちでも地の色は出ない**
- 絵に**隠れ場所や動物を描かない**（別に乗るので二重に見える）。
  画面中央の縦帯（幅の 55%）には主役を置かない

**隠れ場所は画像化していない。** 板1枚にすると
(1) 遮蔽のテストが透明な角に当たって甘くなる
(2) 前板と背板のあいだに動物を挟めなくなる
(3) ふたを開く動きが作れない —— の3つを自分で塞ぐ必要がある。
やるなら「本体（奥）」と「ふた（手前）」の2枚組にして、
隠れ場所側にもアルファのマス目を書き出すこと。

## 見た目の採点（2026-09-07。人間が指示して作った）

隠れ場所の見た目を、**決まった状態で撮った絵と数値**で見る仕組み。
基準と合格ラインは `docs/visual-gate.md`、経過は `docs/visual-log.md`。

```bash
npm run visual:shot                    capture/visual/current/ に7場面
npm run visual:shot -- --out baseline  baseline として撮る
npm run visual:shot -- --verify        2回撮ってバイト単位で一致するか見る
npm run visual:score -- --dir current --vs baseline
```

**CI の合否には使わない。** §10-2 で動いている場面の画素比較は禁止されている。
ここで撮る絵と数値は**人間と `visual-judge` が採点するための材料**で、
`npm run verify` からは呼ばない。**閾値を自分で緩めない**（人間が決める）。

`__peekaboo.freezeAt(id, seed, sec)` が rAF を止め、更新時計を 0 に戻してから
ちょうど n ステップ進めて1枚描く。**壁時計を読まない**ので、GPU の速さで
アニメーションの位相が変わらない。場面ごとにページを読み直す
（続けて切り替えると `generateUUID()` が消費した乱数の回数が変わる）。

場面ごとに3枚撮る。`solid − bg` が隠れ場所の画素、`ふつう − solid` が影の効き。
**影とハローも `solid` から消すこと。** 消し忘れると輪郭のすぐ外の暗がりが
隠れ場所の画素として切り出され、輪郭の明度差が壊れる（実際に踏んだ）。

### 見た目で実測して分かったこと

**平らな面は、光を足しても明るさが一様のまま。**
平行光も半球光も法線しか見ないので、1枚の板の上ではどこも同じ明るさになる。
点光源なら距離で変わるが、面1枚の中では数%しか動かない。
baseline の実測で、はこ・カーテン・ふとん・すいめん・こや の
**内部の L\* の四分位範囲が 0.00〜0.35** だった（＝面が単色）。
**絵の描き手と同じことを頂点カラーでやる**（`FORM_SHADING`）。
上を明るく下を暗く、**横も**（光の側を明るく）、奥ほど暗く。

**縞や斑点で明暗の幅を稼いでも、面は平らに見える。**
木目と斑点を重ねたら数値（V2/V3）は基準を通ったが、判定に
「板の左端 x=25 と x=150 で 0.3/255 しか違わない」と指摘された。
**面の向きに沿った勾配が要る。** 模様はその上の飾り。

**手続き生成の模様は、白から描き始めない。**
白の上に白を重ねても何も変わらないので、模様が「暗くする側」しか効かない。
板ごとの明るさを ±5% 振っているつもりが実際は −5%〜0% しか出ていなかった。
中間の灰（`#c6c6c6`）から描くと幅が倍になる。暗くなるぶんは
**線形空間の平均を測って 1/平均 倍で戻す**（戻さないと場面全体が暗くなる）。

**落ち影は、上の段と下の段で見かけの上下がひっくり返る。**
影は隠れ場所より奥にあるので、カメラから見て画面中心へ寄る
（上の段は下へ、下の段は上へ）。同じだけずらすと下の段で視差に食われ、
影が本体の裏に隠れた（接地の暗さが上の段 20〜28 に対し下の段 0.5〜2.3）。
**見かけの位置で指定して、ローカル座標に逆算する。**
大きさも「倍率」ではなく「芯は本体と同じ、まわりに blur を足す」にする
（倍率 1.06 では 29箇所中 24箇所で影が1画素も見えなかった）。

**背板と内側の板は、四角いままだと丸い塊のまわりに角が残る。**
表面に模様を貼ったとたん、平らな石板・茶色い板としてはっきり見えた。
**楕円にする**（カメラは最大 17° しか回り込まないので隙間はできない）。
**縁より上まで伸ばさない**（きのほら で板塀の上に黒いドームが乗った）。

**場面の光の色は、背景の絵から読める。**
絵の上半分・下半分の平均色を `HemisphereLight(sky, ground)` に渡すだけ。
**絵そのものには光を当てない**（背景も動物も `MeshBasicMaterial`）ので、
色が変わるのは隠れ場所と手続き生成の飾りだけ。
ただし**光の色だけでは彩度のずれは直らない**。ずれは「灰すぎる岩」と
「派手すぎる草」の両方向にあり、光を色づけると後者がさらに押し上がる。
配色も同時に寄せること。

**影を濃くして「接地」を稼がない。** 背景まで暗くするので、
暗い隠れ場所の輪郭がかえって消える（2回とも数値が悪化した）。

**丸い塊に四角い板を1枚でも混ぜると、そこだけ人工物に見える。**
岩の庇を四角い板にしていたら、判定に5場面すべてで
「両端を直角に切り落とした長方形が岩より30px 突き出している」と言われた。
芯の板を残して瘤を重ねた版でも「上下の縁が平行な直線の梁のまま」と、
**同じ減点がそのまま残った**。**直線を1本も残さない**まで直すこと。

**模様は「材質の言い分」を持たせる。** 同じ楕円の斑点を全部に乗せると
「どれも汚れた板」に見える。等間隔に刻むと、板目は**波板シャッター**、
素焼きの溝は**旋盤で挽いたプラスチック**、布の襞は**トタン板**になる。
間隔と太さを不揃いにして、掛け布団とカーテンのように用途が違うものは分ける。

**形を足すときは draw call も数える。** 12×8 の球を7つ「別メッシュで」足したら、
三角形が +30%・draw call が +12% になった（予算は +10%）。
分割を 8×5 に落とし、**7つを1つのジオメトリに結合**して収めた
（three の `BufferGeometryUtils` は examples 側なので、
position / normal / uv だけを繋ぐ短い版を自前で持つ）。

**輪郭の明度差は、勾配を強くするほど通しにくくなる。**
形に沿った明暗を強くすると隠れ場所自身の L\* の幅が広がるので、
背景の明るさがその範囲の内側に入ってしまい、
**輪郭のどこかは必ず背景と同じ明るさになる**。実測で7箇所中4箇所がこれ。
「面を立体に見せる」と「輪郭をはっきりさせる」は、同じ方向ではない。

**場面の光は、色だけでなく向きも絵から読める。**
絵を 24×24 にして、いちばん明るいマスの位置を主光の向きにする。
きょうりゅう の夕日は画面の**左下**（実測 x=34, y=411）にあるのに、
主光はどの場面でも左上に固定で、
「太陽より上に浮いている物体が、太陽と反対向きに陰を持っている」と言われた。
**ただし下からは当てない**（上向き成分に下限を置く）。

**空には影を落とさない。** 背景の絵を横に潰して行ごとの明るさにし、
上下でいちばん大きく変わるところを地平線とみなす。そこより上にある
隠れ場所の影は弱める。のうじょう の こや では影の **52.6%
（20,327px）が空**に落ちていた。**段差の小さい絵（うみ のように
地面が無い場面）では地平線を決めない。**

**e2e の待ちは、見た目を重くするたびに壊れる。**
壁時計で待っている箇所は、模様を貼って描画が重くなると足りなくなる
（1周 4.80秒を 12000ms で待っていて、通し実行のときだけ落ちた）。
`renderer.info.memory` も、数え終わる前に測ると「漏れている」と出る。
**待つのは更新時計、壁時計は固まったときに止めるためだけ**（§10-2）。

## 公開

Netlify（`netlify.toml`）。`main` に push すると自動で公開される。
`BASE_PATH` は指定しない（直下配信）。

**無料プランは右下に「Powered by Netlify」のバッジを挿入する。**
バッジはリンクなので、押すと外部サイトに飛ぶ（不変条件の「外部リンクを置かない」に触れる）。
気にするなら Cloudflare Pages（無料・バッジなし・private リポジトリ可）に移す。

## いまの状態

**Phase 1・Phase 3・Phase 5（動物と場面を増やす）が完了。**
設計書 §12 の Phase 2（登場の粒子）が残っている。

入っているもの:
- `core/` `ui/` `pwa/` の移植（実機で検証済み）
- `SpotSystem` / `AnimalSystem` / `SpotShapes` / `ProceduralAnimals` / `SceneRoot`
- `ChaseSystem`（§4-5 おいかけっこ）/ `EmptySpot`（§4-6 空振り）
- **7場面**（おうち・そと・うみ・のうじょう・どうぶつえん・きょうりゅう
  ＝モードA・各4箇所 / のはら＝モードB・5箇所）
- **動物25体**。`bodyPlan` などで輪郭を作り分ける（`data/animals.ts` を読むこと）
- **参照画像25枚**（`reference/raw/`）と、それに寄せるゲート
  （`docs/match-gate.md` / `npm run ref-split` / `npm run shot` / `npm run match-score`）
- **隠れ場所9種**（はこ・とびら・カーテン・ふとん・くさむら・いわ・すいめん・
  きのほら・うえきばち）
- **場面ごとの味つけ**（`scene/Flavor.ts`）。ゆれる草・かいそう／
  ちょうちょ・小魚が横切る（ときどきとまる）／ため のもぞもぞ／足音／地ひびき／
  あぶく（押すと割れる）／みんなで鳴く／足あと／残る花と卵
- **§6-1 のばらつき**（登場の速さ ±10% / 大きさ ±10% / 声のピッチ ±5%）
- 音: 「ばあっ！」「こっちこっち〜」の声 / ぴょん / ワサワサ / あれ？ / 足音
  （声は `scripts/make-voice.cjs` が生成。**ffmpeg が無くても作れる**ように
  WAV の変換を JS でも書いてある）
- `tests/unit/safety.test.ts` 254件 と E2E 23件
- 素材: 動物の絵25枚（`public/animals/`）と背景7枚（`public/backgrounds/`）。
  **`public/` を空にしても動く**（不変条件7）

入っていないもの:
- `RevealEffect`（登場の粒子。Phase 2）
- **画面上の場面切替バー**（Phase 6）。
  いまは `__peekaboo.setScene('umi')` などでしか場面を変えられない
- `safety.test.ts` に残した `it.todo` 1件（波紋。DOM が要るので E2E 側にある）

### 人間が決めたこと（設計書と違う点）

- **モードB（のはら）はヒントを出さない**（2026-09-05）。
  §4-2 の「隠れている動物は必ず体の一部が見えている」を、
  モードBでは適用しない。外したときに正解の草むらが揺れて教えるので
  （§4-6）、草から体の一部を出す必要がない。§4-5 の表の
  「ヒント＝移動そのもの」とも合う
- **場面を5つにした**（2026-09-05）。§5-3 は4場面だが、のうじょうを足した
- **場面を7つにした**（2026-09-06）。どうぶつえん（ライオン・ぞう・きりん・さる）と
  きょうりゅう（ティラノ・トリケラ・ステゴ・プテラノドン）を足した。
  **モードAの6場面はすべて4箇所のまま**
- **のはらだけ隠れ場所を5箇所にした**（2026-09-05）。§5-3 の
  「4箇所より多くしない」を、モードBでは適用しない。
  行き先が1つ多いほうが追いがいがあるため。**モードAの4場面は4箇所のまま**。
  代わりに当たり判定は 101.4px → 90.1px に縮む（4隅を外へ広げて確保した値）
- **同じ動物を2つの場面に出さない**（2026-09-05）。
  輪郭が似ているだけでも同じに見えるので、指定の組み合わせが
  重なっていないことまでテストで見張る
- **動物は参照画像そのものを板に貼る（道A）**（2026-09-06）。
  手続き生成の球を並べるのをやめ、`public/animals/<id>.webp`（参照画像の
  切り抜き）を1枚の板に貼る（`peekaboo/CutoutAnimal.ts`）。
  **根拠は実測**: この app のカメラは `(0, 0.3, 7.2)` に固定で、
  動物が正面から外れる角度はいちばん深い隠れ場所でも **17.0°**。
  17° で板が縮むのは cos(17°) = 0.956 で 4% しか変わらない。
  **立体であることを、この app はほとんど使っていない。**
  素材が無ければ手続き生成に落ちる（不変条件7）ので `src/peekaboo/
  ProceduralAnimals.ts` は消さないこと。
  副作用として三角形が激減した（おうち 6,722 → 382）。
- **§6-2 は「場面の中で動物を入れ替える」形にした**（2026-09-06）。
  設計書は「`SpotConfig.animals` に複数入れておく」と書いているが、
  **そのままでは動物が足りない**（25体で、モードA 6場面×4箇所にちょうど
  1体ずつ。1箇所に2体ずつ入れるには48体要る）。場面をまたいで使い回すと
  「同じ動物を2つの場面に出さない」を破るので、**場面の中で交換する**。
  交換なので**同時に同じ動物が2箇所に出ることは原理的にない**。
  **毎回入れ替えないこと**（4回に1回）。毎回だと「どこに誰が居るか」を
  形で覚えられなくなる（§5-1 の hintPart の狙いが消える）
- **外したときに「こっちこっち〜」と言う**（2026-09-06）。§4-6 の 0.50s、
  正解の隠れ場所が揺れはじめるのと同じ瞬間。**急かす声にしないこと**
  （外れを「失敗」にしない）。空の隠れ場所はモードBにしか無いので、
  実際に鳴るのは のはら だけ
- **左右のサプライズだけ 1.5倍にした**（2026-09-06）。上下はそのまま。
  **向こう側が画面からはみ出してよい**（画面幅の 1.9倍まで）が、
  入ってくる側の縁にはそろえる
- **サプライズの左右は取り下げた**（2026-09-07）。
  「上下は良いが左右はばあっぽくない」。横から入ってくる動きは
  「ばあ」ではなく「通り過ぎる」に見える。`SIDE_*` の実測は残してある
- **隠れ場所を大きくした**（2026-09-07）。W 1.35 → 1.70 / H 1.05 → 1.35。
  「動物が小さくなった」と言われて測ったら、天井ではなく**隠れ場所そのもの**が
  上限だった。あわせて縦の位置を +1.90 / −2.00 に（上げすぎると画面外に出る）
- **たまごは殻の形そのもので作る**（2026-09-07）。
  四角い前板と背板を足していたら「たまごの前にドアがある」と言われた。
  **割れ目は赤道より上に置く**（同日、実機で「たまごの下半分がドアっぽくて変。
  完全な卵のほうがいい」と言われて直した）。赤道で割ると、いちばん太いところが
  そのまま割れ目に来て**上へすぼまらない**ので、下半分が「上の平らな箱」に見える。
  上下を**同じ楕円体の一部**（割れ目は中心から `RY × 0.24` 上）にすると、
  閉じたときに段差が出ず、輪郭が1つの卵につながる。
  ふたは**画面の中央側へ倒す**（左の列で外へ倒すと画面の外へ出ていた）
- **サプライズは4方向から出る**（2026-09-06）。下・上・左・右。
  **上から出るときだけ上下を反転させる**（反転しないと逆立ちに見える。
  反転すると「画面の外から覗き込んでいる」になる）。
  大きさの基準は向きで変える: 上下は**高さ**の 3/4、左右は**幅**の 3/4。
  左右も高さ基準にすると、縦長の動物が幅いっぱいに広がって
  下から出たときと見分けがつかなかった。
  同じ向きは2回続けない
- **サプライズを「3回に1回・画面下から大きく」にした**（2026-09-06）。
  §6-3 は「10〜30回に1回。頻度は低く保つ」だが、人間が 3回に1回と決めた。
  `Surprise.ts`。**`SURPRISE_CHANCE` は抽選の当たりで、出る割合ではない**
  （2連続で出さないぶん下がるので、見た目 1/3 にするには 0.5 が要る。
  1/3 をそのまま入れた実測は 3.7〜4.0回に1回）。飽きが早ければこの定数を下げる
- **場の見た目に、背景のグラデーションと接地影を入れた（道D）**（2026-09-06）。
  `scene/Backdrop.ts`。**`shadowMap` は使わない**（光源1つ・カメラ固定なので
  影の形が動かない）。影は「地面の影」ではなく**背後の落ち影**にすること。
  水平に寝かせた楕円は見下ろし 11.8° のカメラからは見えず、1枚も映らなかった。
- **場面ごとに「味つけ」を足した**（2026-09-07）。`scene/Flavor.ts`。
  常時のゆれ（草・かいそう）／横切るもの（ちょうちょ・小魚）／
  ため のもぞもぞ／足音／地ひびき の5つで、
  **場面は `SceneConfig.flavor` でデータとして選ぶだけ**。
  `if (scene.id === 'kyoryu')` と書かないこと（動物17体のときと同じ壊れ方をする）
- **1体ずつしか出さない**（2026-09-07）。誰かが出ている（＝隠れきるまでの）
  あいだ、別の隠れ場所を押しても新しくは出さない。1歳半は連打するので、
  4体が同時に出ていると「自分が押したから出た」が読めなくなる。
  **無反応にはしない**（ぷるっ・波紋・音は必ず返るので不変条件1・2 は成り立つ）。
  §4-1 の「押したら出る」だけを人間の判断で変えている
- **たまごは隠れ場所そのものと入れ替える**（2026-09-07）。
  「卵が小さく残っているが何にも意味がない」と言われて、
  引っ込んだあと**その隠れ場所が たまご になる**形にした（`SpotSystem.setShape`）。
  次に押すと殻が横に倒れて動物が出る。1回ぶんで元に戻す
  （戻さないと場面じゅうが たまご になる）。つぼみ（`flower`）も作ったが、
  **花びらが出てきた動物を隠して**見えている割合が 29% まで落ちたので取り下げた
- **出かたの癖は「出たあと」に見せる**（2026-09-07）。
  登場の 0.35秒に癖を足しても目が追いつかず、実機で
  「キリンの首もゾウの鼻も先に出ておらず、普通にばあっするだけ」と言われた。
  出きったあとの 1.6秒（`OUT_IDLE_SEC`）で、きりんは伸び上がり、
  ぞうは左右に傾く。**`out` に入った瞬間は必ず 0**（出きった位置を揃える）
- **足あとは跳ねた道筋に、隠れ場所より手前に置く**（2026-09-07）。
  画面下に横一列で並べた版は「意味のない足あと」と言われた。
  跳んでいる最中（`hop`）だけ落とし、**行き先の近くには落とさない**
  （手前に出すので、着地点に残すと出てくる動物にかぶる）
- **もう1匹が出ている場所を押したら、その子が引っ込む**（2026-09-07）。
  押した先に居るのに §4-6 の空振り（「あれ？」＋正解を教える揺れ）が出るのは
  因果が合わない、と実機で言われた
- **「みんなで鳴く」に「ばあっ！」を使わない**（2026-09-07）。
  隠れたままなのに「ばあっ」と言うのは、いちばん紛らわしい間違いだった。
  `peep`（短い鳴き声）を場所ごとに高さを変えて鳴らす
- **影は、いまその隠れ場所に居る動物の輪郭にする**（2026-09-07）。
  丸ふたつで作ったら「雪だるまみたいで何の意味もない」と言われた
- **味つけに〈中〉の案も足した**（2026-09-07。「大分弱い」と言われて）。
  横切るものがとまる（そと・うみ）／あぶくと、押すと割れる（うみ）／
  みんなで鳴く（のうじょう）／足あと（のはら）／
  引っ込んだあとに花・卵が残る（そと・のうじょう・きょうりゅう）／
  隠れているあいだ、ときどき影だけ映る（おうち）／
  ときどきもう1匹が別のくさむらから顔を出す（のはら。§6 の〈大〉）。
  **「動物のかわりに花が出る」にはしない。** §4-1 の
  「押したら動物が出る」を壊すので、**出たあとに残る**形にした
- **`AnimalConfig.style` を実際に読むようにした**（2026-09-07）。
  25体ぶん書いてあるのに誰も読んでおらず、どの動物も同じ出かたをしていた。
  癖は**位置と傾きのカーブだけ**を変え、**速さ（`reveal` の進みかた）は変えない**
  （§4-3 の山と §4-5 の1周が表からずれるため）。
  `peek` は「顔だけ先に、ゆっくり」で、どうぶつえんの〈中〉案
  「きりんの首だけが上から伸びてくる」はこれで出している。
  **どの癖も reveal が 0 と 1 のときには何も足さない**ので、
  隠れているときと出きったときの見え方は癖を入れる前と同一
- **もう1匹（のはら）は `AnimalSystem` のスロットを使わない**（2026-09-07）。
  使うと `ChaseSystem` と同じ隠れ場所を取り合って、跳ねているうさぎが
  草むらに吸い込まれる（§4-5 の `driven` と同じ話）。
  見た目だけ `SceneRoot` が作って `Flavor` に渡し、
  **本人の居場所と行き先を避け、移動中は出さない**。声も出さない
  （押していないのに「ばあっ！」が鳴ると、どれが自分の押した結果か分からない）
- **影だけは隠れ場所の手前に置く**（2026-09-07）。`Flavor.frontGroup`。
  常設の飾りは「必ず奥」を位置で保証しているが、影はふたに映るものなので
  奥だと見えない。手前に置いてよい理由は位置ではなく**時間**で、
  「隠れているあいだしか出さない」がその保証（テストが見張る）
- **場面ごとに空の色を変えた**（2026-09-07）。`SceneConfig.sky`。
  全部同じ夜空だと、うみ も きょうりゅう も同じ場所に見えていた。
  床の板も 14×10 → 16×16 にした（縦が足りず、上下に地の色の帯が出ていた）
- **§6-1 のばらつきで「ため＋登場」の合計は変えない**（2026-09-07）。
  速さを ±10% 振るが、速くなったぶんだけ ため を伸ばして吸収する。
  合計を振ると §4-3 の 0.50s と §4-5 の 1周 4.80s がその回ごとにずれる
  （実際に安全テストが5件落ちた）。ため の無い呼び戻しでは速い側にしか振らない
- **M1（シルエット一致）の合否は「正面」だけで見る**（2026-09-06）。
  参照の側面図は4本足で立った獣で、アプリの動物は上半身を起こした形なので、
  比べているものが違う（実測: 16体中14体で側面 < 正面。ねこは 0.438）。
  そもそも**アプリのカメラは動物を正面からしか見せない**（出てきた動物は
  必ずカメラを向く）ので、側面は画面に出ない投影を測っている。
  側面は記録するが合否に数えない。体型比率（M2）のほうで使う
- **参照画像との一致ゲートを M1・M2・M5 の3つに絞った**（2026-09-06）。
  参照画像（ChatGPT 生成）は**影も陰影も質感も持たない**フラット塗りなので、
  M4（明暗の構造）は比べる相手が無い。落とした。
  M6（細部の量）は「アプリ ÷ 参照」の比を 1 に近づけると、
  **はりねずみのとげ・うしのぶち・くまのみのしま模様を消す**ことになるので、
  比ではなく「単色の塊になっていないこと」の絶対下限に読み替えた。
  M5（接地影）も参照に影が無いので、一致ではなく絶対基準として残す。
  合格ラインは `docs/match-gate.md`
